/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passthroughUpdatePassthroughRaw } from "../../funcs/passthroughUpdatePassthroughRaw.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdatePassthroughRawRequest$inboundSchema,
};

export const tool$passthroughUpdatePassthroughRaw: ToolDefinition<typeof args> =
  {
    name: "passthrough-update-passthrough-raw",
    description: `Passthrough PUT`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await passthroughUpdatePassthroughRaw(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value?.result;

      return formatResult(value, apiCall);
    },
  };
