/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taskUpdateTaskTask } from "../../funcs/taskUpdateTaskTask.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateTaskTaskRequest$inboundSchema,
};

export const tool$taskUpdateTaskTask: ToolDefinition<typeof args> = {
  name: "task-update-task-task",
  description: `Update a task`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await taskUpdateTaskTask(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
