/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { verificationListVerificationRequests } from "../../funcs/verificationListVerificationRequests.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListVerificationRequestsRequest$inboundSchema,
};

export const tool$verificationListVerificationRequests: ToolDefinition<
  typeof args
> = {
  name: "verification-list-verification-requests",
  description: `List all requests`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await verificationListVerificationRequests(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
