/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lmsPatchLmsClass } from "../../funcs/lmsPatchLmsClass.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchLmsClassRequest$inboundSchema,
};

export const tool$lmsPatchLmsClass: ToolDefinition<typeof args> = {
  name: "lms-patch-lms-class",
  description: `Update a class`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lmsPatchLmsClass(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
