/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passthroughPatchPassthroughRaw } from "../../funcs/passthroughPatchPassthroughRaw.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchPassthroughRawRequest$inboundSchema,
};

export const tool$passthroughPatchPassthroughRaw: ToolDefinition<typeof args> =
  {
    name: "passthrough-patch-passthrough-raw",
    description: `Passthrough PUT`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await passthroughPatchPassthroughRaw(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value?.result;

      return formatResult(value, apiCall);
    },
  };
