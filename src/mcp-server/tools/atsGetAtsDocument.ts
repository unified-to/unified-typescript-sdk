/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsGetAtsDocument } from "../../funcs/atsGetAtsDocument.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAtsDocumentRequest$inboundSchema,
};

export const tool$atsGetAtsDocument: ToolDefinition<typeof args> = {
  name: "ats-get-ats-document",
  description: `Retrieve a document`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsGetAtsDocument(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
