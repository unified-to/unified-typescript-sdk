/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transactionListAccountingTransactions } from "../../funcs/transactionListAccountingTransactions.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingTransactionsRequest$inboundSchema,
};

export const tool$transactionListAccountingTransactions: ToolDefinition<
  typeof args
> = {
  name: "transaction-list-accounting-transactions",
  description: `List all transactions`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transactionListAccountingTransactions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
