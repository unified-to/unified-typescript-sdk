/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyUpdateHrisCompany } from "../../funcs/companyUpdateHrisCompany.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateHrisCompanyRequest$inboundSchema,
};

export const tool$companyUpdateHrisCompany: ToolDefinition<typeof args> = {
  name: "company-update-hris-company",
  description: `Update a company`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyUpdateHrisCompany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
