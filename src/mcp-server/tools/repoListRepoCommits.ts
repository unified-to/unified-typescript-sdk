/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repoListRepoCommits } from "../../funcs/repoListRepoCommits.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListRepoCommitsRequest$inboundSchema,
};

export const tool$repoListRepoCommits: ToolDefinition<typeof args> = {
  name: "repo-list-repo-commits",
  description: `List all commits`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await repoListRepoCommits(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
