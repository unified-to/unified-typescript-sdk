/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customerRemoveTicketingCustomer } from "../../funcs/customerRemoveTicketingCustomer.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveTicketingCustomerRequest$inboundSchema,
};

export const tool$customerRemoveTicketingCustomer: ToolDefinition<typeof args> =
  {
    name: "customer-remove-ticketing-customer",
    description: `Remove a customer`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await customerRemoveTicketingCustomer(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
