/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmRemoveCrmCompany } from "../../funcs/crmRemoveCrmCompany.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveCrmCompanyRequest$inboundSchema,
};

export const tool$crmRemoveCrmCompany: ToolDefinition<typeof args> = {
  name: "crm-remove-crm-company",
  description: `Remove a company`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await crmRemoveCrmCompany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
