/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketGetTicketingTicket } from "../../funcs/ticketGetTicketingTicket.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTicketingTicketRequest$inboundSchema,
};

export const tool$ticketGetTicketingTicket: ToolDefinition<typeof args> = {
  name: "ticket-get-ticketing-ticket",
  description: `Retrieve a ticket`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketGetTicketingTicket(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
