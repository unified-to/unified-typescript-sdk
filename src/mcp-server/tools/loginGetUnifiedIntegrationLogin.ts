/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { loginGetUnifiedIntegrationLogin } from "../../funcs/loginGetUnifiedIntegrationLogin.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUnifiedIntegrationLoginRequest$inboundSchema,
};

export const tool$loginGetUnifiedIntegrationLogin: ToolDefinition<typeof args> =
  {
    name: "login-get-unified-integration-login",
    description: `Sign in a user

Returns an authentication URL for the specified integration.  Once a successful authentication occurs, the name and email are returned inside a jwt parameter, which is a JSON web token that is base-64 encoded.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await loginGetUnifiedIntegrationLogin(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
