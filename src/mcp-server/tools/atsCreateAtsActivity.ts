/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsCreateAtsActivity } from "../../funcs/atsCreateAtsActivity.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateAtsActivityRequest$inboundSchema,
};

export const tool$atsCreateAtsActivity: ToolDefinition<typeof args> = {
  name: "ats-create-ats-activity",
  description: `Create an activity`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsCreateAtsActivity(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
