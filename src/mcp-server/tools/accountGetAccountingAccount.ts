/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountGetAccountingAccount } from "../../funcs/accountGetAccountingAccount.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingAccountRequest$inboundSchema,
};

export const tool$accountGetAccountingAccount: ToolDefinition<typeof args> = {
  name: "account-get-accounting-account",
  description: `Retrieve an account`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountGetAccountingAccount(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
