/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { interviewGetAtsInterview } from "../../funcs/interviewGetAtsInterview.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAtsInterviewRequest$inboundSchema,
};

export const tool$interviewGetAtsInterview: ToolDefinition<typeof args> = {
  name: "interview-get-ats-interview",
  description: `Retrieve an interview`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await interviewGetAtsInterview(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
