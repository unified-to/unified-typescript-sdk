/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repoRemoveRepoBranch } from "../../funcs/repoRemoveRepoBranch.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveRepoBranchRequest$inboundSchema,
};

export const tool$repoRemoveRepoBranch: ToolDefinition<typeof args> = {
  name: "repo-remove-repo-branch",
  description: `Remove a branch`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await repoRemoveRepoBranch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
