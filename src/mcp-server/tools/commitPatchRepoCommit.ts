/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commitPatchRepoCommit } from "../../funcs/commitPatchRepoCommit.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchRepoCommitRequest$inboundSchema,
};

export const tool$commitPatchRepoCommit: ToolDefinition<typeof args> = {
  name: "commit-patch-repo-commit",
  description: `Update a commit`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await commitPatchRepoCommit(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
