/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyCreateHrisCompany } from "../../funcs/companyCreateHrisCompany.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateHrisCompanyRequest$inboundSchema,
};

export const tool$companyCreateHrisCompany: ToolDefinition<typeof args> = {
  name: "company-create-hris-company",
  description: `Create a company`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyCreateHrisCompany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
