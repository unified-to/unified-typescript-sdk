/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scorecardPatchAtsScorecard } from "../../funcs/scorecardPatchAtsScorecard.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchAtsScorecardRequest$inboundSchema,
};

export const tool$scorecardPatchAtsScorecard: ToolDefinition<typeof args> = {
  name: "scorecard-patch-ats-scorecard",
  description: `Update a scorecard`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scorecardPatchAtsScorecard(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
