/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contactGetAccountingContact } from "../../funcs/contactGetAccountingContact.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingContactRequest$inboundSchema,
};

export const tool$contactGetAccountingContact: ToolDefinition<typeof args> = {
  name: "contact-get-accounting-contact",
  description: `Retrieve a contact`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contactGetAccountingContact(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
