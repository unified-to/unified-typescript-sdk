/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectPatchTaskProject } from "../../funcs/projectPatchTaskProject.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchTaskProjectRequest$inboundSchema,
};

export const tool$projectPatchTaskProject: ToolDefinition<typeof args> = {
  name: "project-patch-task-project",
  description: `Update a project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectPatchTaskProject(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
