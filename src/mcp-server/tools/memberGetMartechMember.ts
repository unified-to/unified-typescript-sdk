/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { memberGetMartechMember } from "../../funcs/memberGetMartechMember.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetMartechMemberRequest$inboundSchema,
};

export const tool$memberGetMartechMember: ToolDefinition<typeof args> = {
  name: "member-get-martech-member",
  description: `Retrieve a member`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await memberGetMartechMember(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
