/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { noteGetTicketingNote } from "../../funcs/noteGetTicketingNote.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTicketingNoteRequest$inboundSchema,
};

export const tool$noteGetTicketingNote: ToolDefinition<typeof args> = {
  name: "note-get-ticketing-note",
  description: `Retrieve a note`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await noteGetTicketingNote(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
