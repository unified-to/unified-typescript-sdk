/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { recordingRemoveUcRecording } from "../../funcs/recordingRemoveUcRecording.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveUcRecordingRequest$inboundSchema,
};

export const tool$recordingRemoveUcRecording: ToolDefinition<typeof args> = {
  name: "recording-remove-uc-recording",
  description: `Remove a recording`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await recordingRemoveUcRecording(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
