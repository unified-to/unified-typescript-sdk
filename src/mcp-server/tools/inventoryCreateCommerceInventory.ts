/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { inventoryCreateCommerceInventory } from "../../funcs/inventoryCreateCommerceInventory.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCommerceInventoryRequest$inboundSchema,
};

export const tool$inventoryCreateCommerceInventory: ToolDefinition<
  typeof args
> = {
  name: "inventory-create-commerce-inventory",
  description: `Create an inventory`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await inventoryCreateCommerceInventory(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
