/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { calendarCreateCalendarCalendar } from "../../funcs/calendarCreateCalendarCalendar.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCalendarCalendarRequest$inboundSchema,
};

export const tool$calendarCreateCalendarCalendar: ToolDefinition<typeof args> =
  {
    name: "calendar-create-calendar-calendar",
    description: `Create a calendar`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await calendarCreateCalendarCalendar(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
