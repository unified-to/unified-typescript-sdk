/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pullrequestUpdateRepoPullrequest } from "../../funcs/pullrequestUpdateRepoPullrequest.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateRepoPullrequestRequest$inboundSchema,
};

export const tool$pullrequestUpdateRepoPullrequest: ToolDefinition<
  typeof args
> = {
  name: "pullrequest-update-repo-pullrequest",
  description: `Update a pullrequest`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await pullrequestUpdateRepoPullrequest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
