/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmCreateCrmDeal } from "../../funcs/crmCreateCrmDeal.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCrmDealRequest$inboundSchema,
};

export const tool$crmCreateCrmDeal: ToolDefinition<typeof args> = {
  name: "crm-create-crm-deal",
  description: `Create a deal`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await crmCreateCrmDeal(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
