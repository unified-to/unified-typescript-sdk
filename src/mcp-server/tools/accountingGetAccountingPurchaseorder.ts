/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingGetAccountingPurchaseorder } from "../../funcs/accountingGetAccountingPurchaseorder.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingPurchaseorderRequest$inboundSchema,
};

export const tool$accountingGetAccountingPurchaseorder: ToolDefinition<
  typeof args
> = {
  name: "accounting-get-accounting-purchaseorder",
  description: `Retrieve a purchaseorder`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingGetAccountingPurchaseorder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
