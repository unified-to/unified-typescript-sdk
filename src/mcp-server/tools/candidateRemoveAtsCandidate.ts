/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { candidateRemoveAtsCandidate } from "../../funcs/candidateRemoveAtsCandidate.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveAtsCandidateRequest$inboundSchema,
};

export const tool$candidateRemoveAtsCandidate: ToolDefinition<typeof args> = {
  name: "candidate-remove-ats-candidate",
  description: `Remove a candidate`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await candidateRemoveAtsCandidate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
