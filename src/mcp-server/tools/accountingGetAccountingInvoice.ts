/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingGetAccountingInvoice } from "../../funcs/accountingGetAccountingInvoice.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingInvoiceRequest$inboundSchema,
};

export const tool$accountingGetAccountingInvoice: ToolDefinition<typeof args> =
  {
    name: "accounting-get-accounting-invoice",
    description: `Retrieve an invoice`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await accountingGetAccountingInvoice(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
