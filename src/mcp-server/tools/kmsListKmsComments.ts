/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kmsListKmsComments } from "../../funcs/kmsListKmsComments.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListKmsCommentsRequest$inboundSchema,
};

export const tool$kmsListKmsComments: ToolDefinition<typeof args> = {
  name: "kms-list-kms-comments",
  description: `List all comments`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kmsListKmsComments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
