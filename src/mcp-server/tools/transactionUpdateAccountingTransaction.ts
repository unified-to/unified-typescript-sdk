/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transactionUpdateAccountingTransaction } from "../../funcs/transactionUpdateAccountingTransaction.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAccountingTransactionRequest$inboundSchema,
};

export const tool$transactionUpdateAccountingTransaction: ToolDefinition<
  typeof args
> = {
  name: "transaction-update-accounting-transaction",
  description: `Update a transaction`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transactionUpdateAccountingTransaction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
