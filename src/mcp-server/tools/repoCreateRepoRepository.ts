/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repoCreateRepoRepository } from "../../funcs/repoCreateRepoRepository.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateRepoRepositoryRequest$inboundSchema,
};

export const tool$repoCreateRepoRepository: ToolDefinition<typeof args> = {
  name: "repo-create-repo-repository",
  description: `Create a repository`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await repoCreateRepoRepository(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
