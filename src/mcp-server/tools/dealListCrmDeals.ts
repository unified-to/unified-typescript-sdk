/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dealListCrmDeals } from "../../funcs/dealListCrmDeals.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCrmDealsRequest$inboundSchema,
};

export const tool$dealListCrmDeals: ToolDefinition<typeof args> = {
  name: "deal-list-crm-deals",
  description: `List all deals`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dealListCrmDeals(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
