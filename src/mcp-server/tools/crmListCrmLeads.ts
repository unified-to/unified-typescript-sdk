/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmListCrmLeads } from "../../funcs/crmListCrmLeads.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCrmLeadsRequest$inboundSchema,
};

export const tool$crmListCrmLeads: ToolDefinition<typeof args> = {
  name: "crm-list-crm-leads",
  description: `List all leads`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await crmListCrmLeads(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
