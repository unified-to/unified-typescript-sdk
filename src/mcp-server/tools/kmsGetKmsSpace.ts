/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kmsGetKmsSpace } from "../../funcs/kmsGetKmsSpace.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetKmsSpaceRequest$inboundSchema,
};

export const tool$kmsGetKmsSpace: ToolDefinition<typeof args> = {
  name: "kms-get-kms-space",
  description: `Retrieve a space`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kmsGetKmsSpace(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
