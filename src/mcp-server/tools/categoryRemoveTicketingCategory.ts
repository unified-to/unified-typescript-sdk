/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { categoryRemoveTicketingCategory } from "../../funcs/categoryRemoveTicketingCategory.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveTicketingCategoryRequest$inboundSchema,
};

export const tool$categoryRemoveTicketingCategory: ToolDefinition<typeof args> =
  {
    name: "category-remove-ticketing-category",
    description: `Remove a category`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await categoryRemoveTicketingCategory(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
