/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionListPaymentSubscriptions } from "../../funcs/subscriptionListPaymentSubscriptions.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListPaymentSubscriptionsRequest$inboundSchema,
};

export const tool$subscriptionListPaymentSubscriptions: ToolDefinition<
  typeof args
> = {
  name: "subscription-list-payment-subscriptions",
  description: `List all subscriptions`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await subscriptionListPaymentSubscriptions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
