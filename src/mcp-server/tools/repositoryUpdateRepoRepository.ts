/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repositoryUpdateRepoRepository } from "../../funcs/repositoryUpdateRepoRepository.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateRepoRepositoryRequest$inboundSchema,
};

export const tool$repositoryUpdateRepoRepository: ToolDefinition<typeof args> =
  {
    name: "repository-update-repo-repository",
    description: `Update a repository`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await repositoryUpdateRepoRepository(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
