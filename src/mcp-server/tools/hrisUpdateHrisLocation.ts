/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisUpdateHrisLocation } from "../../funcs/hrisUpdateHrisLocation.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateHrisLocationRequest$inboundSchema,
};

export const tool$hrisUpdateHrisLocation: ToolDefinition<typeof args> = {
  name: "hris-update-hris-location",
  description: `Update a location`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await hrisUpdateHrisLocation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
