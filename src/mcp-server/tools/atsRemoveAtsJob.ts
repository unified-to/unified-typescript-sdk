/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsRemoveAtsJob } from "../../funcs/atsRemoveAtsJob.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveAtsJobRequest$inboundSchema,
};

export const tool$atsRemoveAtsJob: ToolDefinition<typeof args> = {
  name: "ats-remove-ats-job",
  description: `Remove a job`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsRemoveAtsJob(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
