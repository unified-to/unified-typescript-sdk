/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmGetCrmEvent } from "../../funcs/crmGetCrmEvent.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCrmEventRequest$inboundSchema,
};

export const tool$crmGetCrmEvent: ToolDefinition<typeof args> = {
  name: "crm-get-crm-event",
  description: `Retrieve an event`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await crmGetCrmEvent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
