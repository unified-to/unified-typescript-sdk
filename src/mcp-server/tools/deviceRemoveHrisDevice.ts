/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deviceRemoveHrisDevice } from "../../funcs/deviceRemoveHrisDevice.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveHrisDeviceRequest$inboundSchema,
};

export const tool$deviceRemoveHrisDevice: ToolDefinition<typeof args> = {
  name: "device-remove-hris-device",
  description: `Remove a device`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deviceRemoveHrisDevice(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
