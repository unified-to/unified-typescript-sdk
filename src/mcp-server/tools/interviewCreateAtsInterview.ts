/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { interviewCreateAtsInterview } from "../../funcs/interviewCreateAtsInterview.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateAtsInterviewRequest$inboundSchema,
};

export const tool$interviewCreateAtsInterview: ToolDefinition<typeof args> = {
  name: "interview-create-ats-interview",
  description: `Create an interview`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await interviewCreateAtsInterview(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
