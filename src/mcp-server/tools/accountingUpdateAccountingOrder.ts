/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingUpdateAccountingOrder } from "../../funcs/accountingUpdateAccountingOrder.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAccountingOrderRequest$inboundSchema,
};

export const tool$accountingUpdateAccountingOrder: ToolDefinition<typeof args> =
  {
    name: "accounting-update-accounting-order",
    description: `Update an order`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await accountingUpdateAccountingOrder(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
