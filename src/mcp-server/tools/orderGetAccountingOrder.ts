/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { orderGetAccountingOrder } from "../../funcs/orderGetAccountingOrder.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingOrderRequest$inboundSchema,
};

export const tool$orderGetAccountingOrder: ToolDefinition<typeof args> = {
  name: "order-get-accounting-order",
  description: `Retrieve an order`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await orderGetAccountingOrder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
