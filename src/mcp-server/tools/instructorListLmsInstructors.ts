/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { instructorListLmsInstructors } from "../../funcs/instructorListLmsInstructors.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListLmsInstructorsRequest$inboundSchema,
};

export const tool$instructorListLmsInstructors: ToolDefinition<typeof args> = {
  name: "instructor-list-lms-instructors",
  description: `List all instructors`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await instructorListLmsInstructors(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
