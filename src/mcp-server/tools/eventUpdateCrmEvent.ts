/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventUpdateCrmEvent } from "../../funcs/eventUpdateCrmEvent.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateCrmEventRequest$inboundSchema,
};

export const tool$eventUpdateCrmEvent: ToolDefinition<typeof args> = {
  name: "event-update-crm-event",
  description: `Update an event`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await eventUpdateCrmEvent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
