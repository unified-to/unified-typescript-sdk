/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { calendarListCalendarEvents } from "../../funcs/calendarListCalendarEvents.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCalendarEventsRequest$inboundSchema,
};

export const tool$calendarListCalendarEvents: ToolDefinition<typeof args> = {
  name: "calendar-list-calendar-events",
  description: `List all events`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await calendarListCalendarEvents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
