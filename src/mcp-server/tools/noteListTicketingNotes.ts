/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { noteListTicketingNotes } from "../../funcs/noteListTicketingNotes.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListTicketingNotesRequest$inboundSchema,
};

export const tool$noteListTicketingNotes: ToolDefinition<typeof args> = {
  name: "note-list-ticketing-notes",
  description: `List all notes`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await noteListTicketingNotes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
