/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { unifiedListUnifiedApicalls } from "../../funcs/unifiedListUnifiedApicalls.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListUnifiedApicallsRequest$inboundSchema,
};

export const tool$unifiedListUnifiedApicalls: ToolDefinition<typeof args> = {
  name: "unified-list-unified-apicalls",
  description: `Returns API Calls`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await unifiedListUnifiedApicalls(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
