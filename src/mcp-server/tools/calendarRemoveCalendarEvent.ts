/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { calendarRemoveCalendarEvent } from "../../funcs/calendarRemoveCalendarEvent.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveCalendarEventRequest$inboundSchema,
};

export const tool$calendarRemoveCalendarEvent: ToolDefinition<typeof args> = {
  name: "calendar-remove-calendar-event",
  description: `Remove an event`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await calendarRemoveCalendarEvent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
