/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { locationRemoveCommerceLocation } from "../../funcs/locationRemoveCommerceLocation.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveCommerceLocationRequest$inboundSchema,
};

export const tool$locationRemoveCommerceLocation: ToolDefinition<typeof args> =
  {
    name: "location-remove-commerce-location",
    description: `Remove a location`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await locationRemoveCommerceLocation(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
