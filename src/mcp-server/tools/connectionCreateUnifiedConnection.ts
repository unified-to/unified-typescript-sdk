/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectionCreateUnifiedConnection } from "../../funcs/connectionCreateUnifiedConnection.js";
import * as shared from "../../sdk/models/shared/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: shared.Connection$inboundSchema,
};

export const tool$connectionCreateUnifiedConnection: ToolDefinition<
  typeof args
> = {
  name: "connection-create-unified-connection",
  description: `Create connection

Used only to import existing customer credentials; use "Create connection indirectly" instead`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await connectionCreateUnifiedConnection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
