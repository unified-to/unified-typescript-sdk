/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingGetAccountingReport } from "../../funcs/accountingGetAccountingReport.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingReportRequest$inboundSchema,
};

export const tool$accountingGetAccountingReport: ToolDefinition<typeof args> = {
  name: "accounting-get-accounting-report",
  description: `Retrieve a report`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingGetAccountingReport(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
