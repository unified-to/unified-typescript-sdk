/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { listUpdateMartechList } from "../../funcs/listUpdateMartechList.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateMartechListRequest$inboundSchema,
};

export const tool$listUpdateMartechList: ToolDefinition<typeof args> = {
  name: "list-update-martech-list",
  description: `Update a list`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await listUpdateMartechList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
