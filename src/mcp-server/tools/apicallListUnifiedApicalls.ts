/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apicallListUnifiedApicalls } from "../../funcs/apicallListUnifiedApicalls.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListUnifiedApicallsRequest$inboundSchema,
};

export const tool$apicallListUnifiedApicalls: ToolDefinition<typeof args> = {
  name: "apicall-list-unified-apicalls",
  description: `Returns API Calls`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await apicallListUnifiedApicalls(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
