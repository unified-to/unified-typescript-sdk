/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lmsRemoveLmsInstructor } from "../../funcs/lmsRemoveLmsInstructor.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveLmsInstructorRequest$inboundSchema,
};

export const tool$lmsRemoveLmsInstructor: ToolDefinition<typeof args> = {
  name: "lms-remove-lms-instructor",
  description: `Remove an instructor`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lmsRemoveLmsInstructor(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
