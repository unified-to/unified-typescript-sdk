/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passthroughRemovePassthrough } from "../../funcs/passthroughRemovePassthrough.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemovePassthroughRequest$inboundSchema,
};

export const tool$passthroughRemovePassthrough: ToolDefinition<typeof args> = {
  name: "passthrough-remove-passthrough",
  description: `Passthrough DELETE`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await passthroughRemovePassthrough(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value?.result;

    return formatResult(value, apiCall);
  },
};
