/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyCreateCrmCompany } from "../../funcs/companyCreateCrmCompany.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCrmCompanyRequest$inboundSchema,
};

export const tool$companyCreateCrmCompany: ToolDefinition<typeof args> = {
  name: "company-create-crm-company",
  description: `Create a company`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyCreateCrmCompany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
