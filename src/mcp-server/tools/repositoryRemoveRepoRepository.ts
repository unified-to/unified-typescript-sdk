/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repositoryRemoveRepoRepository } from "../../funcs/repositoryRemoveRepoRepository.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveRepoRepositoryRequest$inboundSchema,
};

export const tool$repositoryRemoveRepoRepository: ToolDefinition<typeof args> =
  {
    name: "repository-remove-repo-repository",
    description: `Remove a repository`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await repositoryRemoveRepoRepository(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
