/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmRemoveCrmPipeline } from "../../funcs/crmRemoveCrmPipeline.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveCrmPipelineRequest$inboundSchema,
};

export const tool$crmRemoveCrmPipeline: ToolDefinition<typeof args> = {
  name: "crm-remove-crm-pipeline",
  description: `Remove a pipeline`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await crmRemoveCrmPipeline(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
