/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionGetPaymentSubscription } from "../../funcs/subscriptionGetPaymentSubscription.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPaymentSubscriptionRequest$inboundSchema,
};

export const tool$subscriptionGetPaymentSubscription: ToolDefinition<
  typeof args
> = {
  name: "subscription-get-payment-subscription",
  description: `Retrieve a subscription`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await subscriptionGetPaymentSubscription(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
