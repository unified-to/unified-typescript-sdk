/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationCreateRepoOrganization } from "../../funcs/organizationCreateRepoOrganization.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateRepoOrganizationRequest$inboundSchema,
};

export const tool$organizationCreateRepoOrganization: ToolDefinition<
  typeof args
> = {
  name: "organization-create-repo-organization",
  description: `Create an organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationCreateRepoOrganization(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
