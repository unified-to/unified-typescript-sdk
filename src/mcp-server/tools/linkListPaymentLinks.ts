/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkListPaymentLinks } from "../../funcs/linkListPaymentLinks.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListPaymentLinksRequest$inboundSchema,
};

export const tool$linkListPaymentLinks: ToolDefinition<typeof args> = {
  name: "link-list-payment-links",
  description: `List all links`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await linkListPaymentLinks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
