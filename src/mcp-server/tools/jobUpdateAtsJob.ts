/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { jobUpdateAtsJob } from "../../funcs/jobUpdateAtsJob.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAtsJobRequest$inboundSchema,
};

export const tool$jobUpdateAtsJob: ToolDefinition<typeof args> = {
  name: "job-update-ats-job",
  description: `Update a job`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await jobUpdateAtsJob(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
