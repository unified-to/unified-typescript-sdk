/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentCreateAtsDocument } from "../../funcs/documentCreateAtsDocument.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateAtsDocumentRequest$inboundSchema,
};

export const tool$documentCreateAtsDocument: ToolDefinition<typeof args> = {
  name: "document-create-ats-document",
  description: `Create a document`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentCreateAtsDocument(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
