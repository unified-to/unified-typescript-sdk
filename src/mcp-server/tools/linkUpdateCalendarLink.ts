/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkUpdateCalendarLink } from "../../funcs/linkUpdateCalendarLink.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateCalendarLinkRequest$inboundSchema,
};

export const tool$linkUpdateCalendarLink: ToolDefinition<typeof args> = {
  name: "link-update-calendar-link",
  description: `Update a link`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await linkUpdateCalendarLink(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
