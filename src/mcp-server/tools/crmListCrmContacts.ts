/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmListCrmContacts } from "../../funcs/crmListCrmContacts.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCrmContactsRequest$inboundSchema,
};

export const tool$crmListCrmContacts: ToolDefinition<typeof args> = {
  name: "crm-list-crm-contacts",
  description: `List all contacts`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await crmListCrmContacts(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
