/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationGetAccountingOrganization } from "../../funcs/organizationGetAccountingOrganization.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingOrganizationRequest$inboundSchema,
};

export const tool$organizationGetAccountingOrganization: ToolDefinition<
  typeof args
> = {
  name: "organization-get-accounting-organization",
  description: `Retrieve an organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationGetAccountingOrganization(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
