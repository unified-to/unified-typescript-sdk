/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingListAccountingJournals } from "../../funcs/accountingListAccountingJournals.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingJournalsRequest$inboundSchema,
};

export const tool$accountingListAccountingJournals: ToolDefinition<
  typeof args
> = {
  name: "accounting-list-accounting-journals",
  description: `List all journals`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingListAccountingJournals(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
