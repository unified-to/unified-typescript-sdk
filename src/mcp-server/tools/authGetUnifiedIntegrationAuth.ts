/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { authGetUnifiedIntegrationAuth } from "../../funcs/authGetUnifiedIntegrationAuth.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUnifiedIntegrationAuthRequest$inboundSchema,
};

export const tool$authGetUnifiedIntegrationAuth: ToolDefinition<typeof args> = {
  name: "auth-get-unified-integration-auth",
  description: `Create connection indirectly

Returns an authorization URL for the specified integration.  Once a successful authorization occurs, a new connection is created.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await authGetUnifiedIntegrationAuth(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
