/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pipelineGetCrmPipeline } from "../../funcs/pipelineGetCrmPipeline.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCrmPipelineRequest$inboundSchema,
};

export const tool$pipelineGetCrmPipeline: ToolDefinition<typeof args> = {
  name: "pipeline-get-crm-pipeline",
  description: `Retrieve a pipeline`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await pipelineGetCrmPipeline(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
