/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentRemovePaymentSubscription } from "../../funcs/paymentRemovePaymentSubscription.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemovePaymentSubscriptionRequest$inboundSchema,
};

export const tool$paymentRemovePaymentSubscription: ToolDefinition<
  typeof args
> = {
  name: "payment-remove-payment-subscription",
  description: `Remove a subscription`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentRemovePaymentSubscription(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
