/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingListAccountingSalesorders } from "../../funcs/accountingListAccountingSalesorders.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingSalesordersRequest$inboundSchema,
};

export const tool$accountingListAccountingSalesorders: ToolDefinition<
  typeof args
> = {
  name: "accounting-list-accounting-salesorders",
  description: `List all salesorders`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingListAccountingSalesorders(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
