/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taskRemoveTaskComment } from "../../funcs/taskRemoveTaskComment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveTaskCommentRequest$inboundSchema,
};

export const tool$taskRemoveTaskComment: ToolDefinition<typeof args> = {
  name: "task-remove-task-comment",
  description: `Remove a comment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await taskRemoveTaskComment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
