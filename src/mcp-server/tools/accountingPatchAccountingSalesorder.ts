/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingPatchAccountingSalesorder } from "../../funcs/accountingPatchAccountingSalesorder.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchAccountingSalesorderRequest$inboundSchema,
};

export const tool$accountingPatchAccountingSalesorder: ToolDefinition<
  typeof args
> = {
  name: "accounting-patch-accounting-salesorder",
  description: `Update a salesorder`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingPatchAccountingSalesorder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
