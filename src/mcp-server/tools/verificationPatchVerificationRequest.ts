/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { verificationPatchVerificationRequest } from "../../funcs/verificationPatchVerificationRequest.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchVerificationRequestRequest$inboundSchema,
};

export const tool$verificationPatchVerificationRequest: ToolDefinition<
  typeof args
> = {
  name: "verification-patch-verification-request",
  description: `Update a request`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await verificationPatchVerificationRequest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
