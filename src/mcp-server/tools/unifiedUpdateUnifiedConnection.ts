/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { unifiedUpdateUnifiedConnection } from "../../funcs/unifiedUpdateUnifiedConnection.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateUnifiedConnectionRequest$inboundSchema,
};

export const tool$unifiedUpdateUnifiedConnection: ToolDefinition<typeof args> =
  {
    name: "unified-update-unified-connection",
    description: `Update connection`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await unifiedUpdateUnifiedConnection(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
