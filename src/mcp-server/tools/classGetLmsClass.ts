/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classGetLmsClass } from "../../funcs/classGetLmsClass.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetLmsClassRequest$inboundSchema,
};

export const tool$classGetLmsClass: ToolDefinition<typeof args> = {
  name: "class-get-lms-class",
  description: `Retrieve a class`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await classGetLmsClass(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
