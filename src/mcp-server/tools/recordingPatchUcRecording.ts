/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { recordingPatchUcRecording } from "../../funcs/recordingPatchUcRecording.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchUcRecordingRequest$inboundSchema,
};

export const tool$recordingPatchUcRecording: ToolDefinition<typeof args> = {
  name: "recording-patch-uc-recording",
  description: `Update a recording`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await recordingPatchUcRecording(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
