/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { studentCreateLmsStudent } from "../../funcs/studentCreateLmsStudent.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateLmsStudentRequest$inboundSchema,
};

export const tool$studentCreateLmsStudent: ToolDefinition<typeof args> = {
  name: "student-create-lms-student",
  description: `Create a student`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await studentCreateLmsStudent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
