/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingListAccountingContacts } from "../../funcs/accountingListAccountingContacts.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingContactsRequest$inboundSchema,
};

export const tool$accountingListAccountingContacts: ToolDefinition<
  typeof args
> = {
  name: "accounting-list-accounting-contacts",
  description: `List all contacts`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingListAccountingContacts(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
