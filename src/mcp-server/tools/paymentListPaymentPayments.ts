/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentListPaymentPayments } from "../../funcs/paymentListPaymentPayments.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListPaymentPaymentsRequest$inboundSchema,
};

export const tool$paymentListPaymentPayments: ToolDefinition<typeof args> = {
  name: "payment-list-payment-payments",
  description: `List all payments`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentListPaymentPayments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
