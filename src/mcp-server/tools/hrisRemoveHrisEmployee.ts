/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisRemoveHrisEmployee } from "../../funcs/hrisRemoveHrisEmployee.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveHrisEmployeeRequest$inboundSchema,
};

export const tool$hrisRemoveHrisEmployee: ToolDefinition<typeof args> = {
  name: "hris-remove-hris-employee",
  description: `Remove an employee`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await hrisRemoveHrisEmployee(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
