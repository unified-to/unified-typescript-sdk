/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsUpdateAtsDocument } from "../../funcs/atsUpdateAtsDocument.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAtsDocumentRequest$inboundSchema,
};

export const tool$atsUpdateAtsDocument: ToolDefinition<typeof args> = {
  name: "ats-update-ats-document",
  description: `Update a document`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsUpdateAtsDocument(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
