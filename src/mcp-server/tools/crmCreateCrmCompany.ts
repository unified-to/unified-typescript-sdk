/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmCreateCrmCompany } from "../../funcs/crmCreateCrmCompany.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCrmCompanyRequest$inboundSchema,
};

export const tool$crmCreateCrmCompany: ToolDefinition<typeof args> = {
  name: "crm-create-crm-company",
  description: `Create a company`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await crmCreateCrmCompany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
