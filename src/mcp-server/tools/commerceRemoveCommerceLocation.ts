/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commerceRemoveCommerceLocation } from "../../funcs/commerceRemoveCommerceLocation.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveCommerceLocationRequest$inboundSchema,
};

export const tool$commerceRemoveCommerceLocation: ToolDefinition<typeof args> =
  {
    name: "commerce-remove-commerce-location",
    description: `Remove a location`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await commerceRemoveCommerceLocation(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
