/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelGetMessagingChannel } from "../../funcs/channelGetMessagingChannel.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetMessagingChannelRequest$inboundSchema,
};

export const tool$channelGetMessagingChannel: ToolDefinition<typeof args> = {
  name: "channel-get-messaging-channel",
  description: `Retrieve a channel`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelGetMessagingChannel(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
