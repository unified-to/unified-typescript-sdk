/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commerceUpdateCommerceInventory } from "../../funcs/commerceUpdateCommerceInventory.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateCommerceInventoryRequest$inboundSchema,
};

export const tool$commerceUpdateCommerceInventory: ToolDefinition<typeof args> =
  {
    name: "commerce-update-commerce-inventory",
    description: `Update an inventory`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await commerceUpdateCommerceInventory(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
