/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invoiceUpdateAccountingInvoice } from "../../funcs/invoiceUpdateAccountingInvoice.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAccountingInvoiceRequest$inboundSchema,
};

export const tool$invoiceUpdateAccountingInvoice: ToolDefinition<typeof args> =
  {
    name: "invoice-update-accounting-invoice",
    description: `Update an invoice`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await invoiceUpdateAccountingInvoice(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
