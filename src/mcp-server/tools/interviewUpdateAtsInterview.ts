/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { interviewUpdateAtsInterview } from "../../funcs/interviewUpdateAtsInterview.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAtsInterviewRequest$inboundSchema,
};

export const tool$interviewUpdateAtsInterview: ToolDefinition<typeof args> = {
  name: "interview-update-ats-interview",
  description: `Update an interview`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await interviewUpdateAtsInterview(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
