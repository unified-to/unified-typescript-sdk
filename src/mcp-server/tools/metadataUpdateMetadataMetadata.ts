/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metadataUpdateMetadataMetadata } from "../../funcs/metadataUpdateMetadataMetadata.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateMetadataMetadataRequest$inboundSchema,
};

export const tool$metadataUpdateMetadataMetadata: ToolDefinition<typeof args> =
  {
    name: "metadata-update-metadata-metadata",
    description: `Update a metadata`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await metadataUpdateMetadataMetadata(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
