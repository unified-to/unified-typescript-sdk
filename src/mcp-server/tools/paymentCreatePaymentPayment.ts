/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentCreatePaymentPayment } from "../../funcs/paymentCreatePaymentPayment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreatePaymentPaymentRequest$inboundSchema,
};

export const tool$paymentCreatePaymentPayment: ToolDefinition<typeof args> = {
  name: "payment-create-payment-payment",
  description: `Create a payment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentCreatePaymentPayment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
