/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsGetAtsCompany } from "../../funcs/atsGetAtsCompany.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAtsCompanyRequest$inboundSchema,
};

export const tool$atsGetAtsCompany: ToolDefinition<typeof args> = {
  name: "ats-get-ats-company",
  description: `Retrieve a company`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsGetAtsCompany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
