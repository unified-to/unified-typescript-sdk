/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scimCreateScimGroups } from "../../funcs/scimCreateScimGroups.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateScimGroupsRequest$inboundSchema,
};

export const tool$scimCreateScimGroups: ToolDefinition<typeof args> = {
  name: "scim-create-scim-groups",
  description: `Create group`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scimCreateScimGroups(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
