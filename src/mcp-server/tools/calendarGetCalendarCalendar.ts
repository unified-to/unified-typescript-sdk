/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { calendarGetCalendarCalendar } from "../../funcs/calendarGetCalendarCalendar.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCalendarCalendarRequest$inboundSchema,
};

export const tool$calendarGetCalendarCalendar: ToolDefinition<typeof args> = {
  name: "calendar-get-calendar-calendar",
  description: `Retrieve a calendar`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await calendarGetCalendarCalendar(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
