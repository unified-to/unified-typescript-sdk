/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { martechPatchMartechList } from "../../funcs/martechPatchMartechList.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchMartechListRequest$inboundSchema,
};

export const tool$martechPatchMartechList: ToolDefinition<typeof args> = {
  name: "martech-patch-martech-list",
  description: `Update a list`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await martechPatchMartechList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
