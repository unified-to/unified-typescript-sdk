/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisPatchHrisLocation } from "../../funcs/hrisPatchHrisLocation.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchHrisLocationRequest$inboundSchema,
};

export const tool$hrisPatchHrisLocation: ToolDefinition<typeof args> = {
  name: "hris-patch-hris-location",
  description: `Update a location`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await hrisPatchHrisLocation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
