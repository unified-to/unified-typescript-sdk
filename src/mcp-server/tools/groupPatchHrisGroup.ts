/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupPatchHrisGroup } from "../../funcs/groupPatchHrisGroup.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchHrisGroupRequest$inboundSchema,
};

export const tool$groupPatchHrisGroup: ToolDefinition<typeof args> = {
  name: "group-patch-hris-group",
  description: `Update a group`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupPatchHrisGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
