/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taskUpdateTaskComment } from "../../funcs/taskUpdateTaskComment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateTaskCommentRequest$inboundSchema,
};

export const tool$taskUpdateTaskComment: ToolDefinition<typeof args> = {
  name: "task-update-task-comment",
  description: `Update a comment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await taskUpdateTaskComment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
