/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { unifiedGetUnifiedIntegrationAuth } from "../../funcs/unifiedGetUnifiedIntegrationAuth.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUnifiedIntegrationAuthRequest$inboundSchema,
};

export const tool$unifiedGetUnifiedIntegrationAuth: ToolDefinition<
  typeof args
> = {
  name: "unified-get-unified-integration-auth",
  description: `Create connection indirectly

Returns an authorization URL for the specified integration.  Once a successful authorization occurs, a new connection is created.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await unifiedGetUnifiedIntegrationAuth(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
