/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ucListUcContacts } from "../../funcs/ucListUcContacts.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListUcContactsRequest$inboundSchema,
};

export const tool$ucListUcContacts: ToolDefinition<typeof args> = {
  name: "uc-list-uc-contacts",
  description: `List all contacts`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ucListUcContacts(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
