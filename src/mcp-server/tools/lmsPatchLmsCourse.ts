/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lmsPatchLmsCourse } from "../../funcs/lmsPatchLmsCourse.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchLmsCourseRequest$inboundSchema,
};

export const tool$lmsPatchLmsCourse: ToolDefinition<typeof args> = {
  name: "lms-patch-lms-course",
  description: `Update a course`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lmsPatchLmsCourse(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
