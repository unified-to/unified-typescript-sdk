/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taskCreateTaskComment } from "../../funcs/taskCreateTaskComment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateTaskCommentRequest$inboundSchema,
};

export const tool$taskCreateTaskComment: ToolDefinition<typeof args> = {
  name: "task-create-task-comment",
  description: `Create a comment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await taskCreateTaskComment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
