/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repoGetRepoRepository } from "../../funcs/repoGetRepoRepository.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetRepoRepositoryRequest$inboundSchema,
};

export const tool$repoGetRepoRepository: ToolDefinition<typeof args> = {
  name: "repo-get-repo-repository",
  description: `Retrieve a repository`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await repoGetRepoRepository(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
