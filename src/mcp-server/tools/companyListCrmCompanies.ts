/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyListCrmCompanies } from "../../funcs/companyListCrmCompanies.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCrmCompaniesRequest$inboundSchema,
};

export const tool$companyListCrmCompanies: ToolDefinition<typeof args> = {
  name: "company-list-crm-companies",
  description: `List all companies`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyListCrmCompanies(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
