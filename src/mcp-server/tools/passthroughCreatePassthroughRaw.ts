/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passthroughCreatePassthroughRaw } from "../../funcs/passthroughCreatePassthroughRaw.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreatePassthroughRawRequest$inboundSchema,
};

export const tool$passthroughCreatePassthroughRaw: ToolDefinition<typeof args> =
  {
    name: "passthrough-create-passthrough-raw",
    description: `Passthrough POST`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await passthroughCreatePassthroughRaw(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value?.result;

      return formatResult(value, apiCall);
    },
  };
