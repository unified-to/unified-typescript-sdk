/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taxrateListAccountingTaxrates } from "../../funcs/taxrateListAccountingTaxrates.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingTaxratesRequest$inboundSchema,
};

export const tool$taxrateListAccountingTaxrates: ToolDefinition<typeof args> = {
  name: "taxrate-list-accounting-taxrates",
  description: `List all taxrates`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await taxrateListAccountingTaxrates(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
