/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { itemRemoveCommerceItem } from "../../funcs/itemRemoveCommerceItem.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveCommerceItemRequest$inboundSchema,
};

export const tool$itemRemoveCommerceItem: ToolDefinition<typeof args> = {
  name: "item-remove-commerce-item",
  description: `Remove an item`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await itemRemoveCommerceItem(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
