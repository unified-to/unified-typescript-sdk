/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { interviewPatchAtsInterview } from "../../funcs/interviewPatchAtsInterview.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchAtsInterviewRequest$inboundSchema,
};

export const tool$interviewPatchAtsInterview: ToolDefinition<typeof args> = {
  name: "interview-patch-ats-interview",
  description: `Update an interview`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await interviewPatchAtsInterview(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
