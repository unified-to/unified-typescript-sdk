/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taskRemoveTaskTask } from "../../funcs/taskRemoveTaskTask.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveTaskTaskRequest$inboundSchema,
};

export const tool$taskRemoveTaskTask: ToolDefinition<typeof args> = {
  name: "task-remove-task-task",
  description: `Remove a task`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await taskRemoveTaskTask(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
