/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingRemoveAccountingPurchaseorder } from "../../funcs/accountingRemoveAccountingPurchaseorder.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveAccountingPurchaseorderRequest$inboundSchema,
};

export const tool$accountingRemoveAccountingPurchaseorder: ToolDefinition<
  typeof args
> = {
  name: "accounting-remove-accounting-purchaseorder",
  description: `Remove a purchaseorder`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingRemoveAccountingPurchaseorder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
