/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { storageGetStorageFile } from "../../funcs/storageGetStorageFile.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetStorageFileRequest$inboundSchema,
};

export const tool$storageGetStorageFile: ToolDefinition<typeof args> = {
  name: "storage-get-storage-file",
  description: `Retrieve a file`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await storageGetStorageFile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
