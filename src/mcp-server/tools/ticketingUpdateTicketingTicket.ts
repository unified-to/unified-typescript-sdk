/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingUpdateTicketingTicket } from "../../funcs/ticketingUpdateTicketingTicket.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateTicketingTicketRequest$inboundSchema,
};

export const tool$ticketingUpdateTicketingTicket: ToolDefinition<typeof args> =
  {
    name: "ticketing-update-ticketing-ticket",
    description: `Update a ticket`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await ticketingUpdateTicketingTicket(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
