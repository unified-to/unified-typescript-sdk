/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyListAtsCompanies } from "../../funcs/companyListAtsCompanies.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAtsCompaniesRequest$inboundSchema,
};

export const tool$companyListAtsCompanies: ToolDefinition<typeof args> = {
  name: "company-list-ats-companies",
  description: `List all companies`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyListAtsCompanies(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
