/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ucPatchUcContact } from "../../funcs/ucPatchUcContact.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchUcContactRequest$inboundSchema,
};

export const tool$ucPatchUcContact: ToolDefinition<typeof args> = {
  name: "uc-patch-uc-contact",
  description: `Update a contact`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ucPatchUcContact(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
