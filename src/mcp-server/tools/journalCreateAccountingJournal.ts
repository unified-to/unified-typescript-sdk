/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { journalCreateAccountingJournal } from "../../funcs/journalCreateAccountingJournal.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateAccountingJournalRequest$inboundSchema,
};

export const tool$journalCreateAccountingJournal: ToolDefinition<typeof args> =
  {
    name: "journal-create-accounting-journal",
    description: `Create a journal`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await journalCreateAccountingJournal(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
