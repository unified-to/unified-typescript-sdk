/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { calendarListCalendarRecordings } from "../../funcs/calendarListCalendarRecordings.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCalendarRecordingsRequest$inboundSchema,
};

export const tool$calendarListCalendarRecordings: ToolDefinition<typeof args> =
  {
    name: "calendar-list-calendar-recordings",
    description: `List all recordings`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await calendarListCalendarRecordings(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
