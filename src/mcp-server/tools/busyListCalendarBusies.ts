/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { busyListCalendarBusies } from "../../funcs/busyListCalendarBusies.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCalendarBusiesRequest$inboundSchema,
};

export const tool$busyListCalendarBusies: ToolDefinition<typeof args> = {
  name: "busy-list-calendar-busies",
  description: `List all busies`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await busyListCalendarBusies(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
