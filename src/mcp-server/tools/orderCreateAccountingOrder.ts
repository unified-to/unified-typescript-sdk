/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { orderCreateAccountingOrder } from "../../funcs/orderCreateAccountingOrder.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateAccountingOrderRequest$inboundSchema,
};

export const tool$orderCreateAccountingOrder: ToolDefinition<typeof args> = {
  name: "order-create-accounting-order",
  description: `Create an order`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await orderCreateAccountingOrder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
