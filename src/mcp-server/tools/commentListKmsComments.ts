/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commentListKmsComments } from "../../funcs/commentListKmsComments.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListKmsCommentsRequest$inboundSchema,
};

export const tool$commentListKmsComments: ToolDefinition<typeof args> = {
  name: "comment-list-kms-comments",
  description: `List all comments`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await commentListKmsComments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
