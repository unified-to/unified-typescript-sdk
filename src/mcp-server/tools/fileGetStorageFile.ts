/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fileGetStorageFile } from "../../funcs/fileGetStorageFile.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetStorageFileRequest$inboundSchema,
};

export const tool$fileGetStorageFile: ToolDefinition<typeof args> = {
  name: "file-get-storage-file",
  description: `Retrieve a file`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await fileGetStorageFile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
