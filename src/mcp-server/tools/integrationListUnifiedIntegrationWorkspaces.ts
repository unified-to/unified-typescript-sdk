/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationListUnifiedIntegrationWorkspaces } from "../../funcs/integrationListUnifiedIntegrationWorkspaces.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListUnifiedIntegrationWorkspacesRequest$inboundSchema,
};

export const tool$integrationListUnifiedIntegrationWorkspaces: ToolDefinition<
  typeof args
> = {
  name: "integration-list-unified-integration-workspaces",
  description: `Returns all activated integrations in a workspace

No authentication required as this is to be used by front-end interface`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationListUnifiedIntegrationWorkspaces(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
