/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { storagePatchStorageFile } from "../../funcs/storagePatchStorageFile.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchStorageFileRequest$inboundSchema,
};

export const tool$storagePatchStorageFile: ToolDefinition<typeof args> = {
  name: "storage-patch-storage-file",
  description: `Update a file`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await storagePatchStorageFile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
