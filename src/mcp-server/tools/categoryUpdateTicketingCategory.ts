/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { categoryUpdateTicketingCategory } from "../../funcs/categoryUpdateTicketingCategory.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateTicketingCategoryRequest$inboundSchema,
};

export const tool$categoryUpdateTicketingCategory: ToolDefinition<typeof args> =
  {
    name: "category-update-ticketing-category",
    description: `Update a category`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await categoryUpdateTicketingCategory(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
