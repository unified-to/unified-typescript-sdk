/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kmsCreateKmsPage } from "../../funcs/kmsCreateKmsPage.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateKmsPageRequest$inboundSchema,
};

export const tool$kmsCreateKmsPage: ToolDefinition<typeof args> = {
  name: "kms-create-kms-page",
  description: `Create a page`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kmsCreateKmsPage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
