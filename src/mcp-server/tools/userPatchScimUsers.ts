/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userPatchScimUsers } from "../../funcs/userPatchScimUsers.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchScimUsersRequest$inboundSchema,
};

export const tool$userPatchScimUsers: ToolDefinition<typeof args> = {
  name: "user-patch-scim-users",
  description: `Update user`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await userPatchScimUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
