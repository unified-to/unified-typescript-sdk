/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { martechListMartechLists } from "../../funcs/martechListMartechLists.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListMartechListsRequest$inboundSchema,
};

export const tool$martechListMartechLists: ToolDefinition<typeof args> = {
  name: "martech-list-martech-lists",
  description: `List all lists`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await martechListMartechLists(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
