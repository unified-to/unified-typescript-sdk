/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taxrateGetAccountingTaxrate } from "../../funcs/taxrateGetAccountingTaxrate.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingTaxrateRequest$inboundSchema,
};

export const tool$taxrateGetAccountingTaxrate: ToolDefinition<typeof args> = {
  name: "taxrate-get-accounting-taxrate",
  description: `Retrieve a taxrate`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await taxrateGetAccountingTaxrate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
