/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taskUpdateTaskProject } from "../../funcs/taskUpdateTaskProject.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateTaskProjectRequest$inboundSchema,
};

export const tool$taskUpdateTaskProject: ToolDefinition<typeof args> = {
  name: "task-update-task-project",
  description: `Update a project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await taskUpdateTaskProject(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
