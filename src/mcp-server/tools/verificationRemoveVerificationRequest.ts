/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { verificationRemoveVerificationRequest } from "../../funcs/verificationRemoveVerificationRequest.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveVerificationRequestRequest$inboundSchema,
};

export const tool$verificationRemoveVerificationRequest: ToolDefinition<
  typeof args
> = {
  name: "verification-remove-verification-request",
  description: `Remove a request`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await verificationRemoveVerificationRequest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
