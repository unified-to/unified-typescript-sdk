/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { studentUpdateLmsStudent } from "../../funcs/studentUpdateLmsStudent.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateLmsStudentRequest$inboundSchema,
};

export const tool$studentUpdateLmsStudent: ToolDefinition<typeof args> = {
  name: "student-update-lms-student",
  description: `Update a student`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await studentUpdateLmsStudent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
