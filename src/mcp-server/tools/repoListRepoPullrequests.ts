/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repoListRepoPullrequests } from "../../funcs/repoListRepoPullrequests.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListRepoPullrequestsRequest$inboundSchema,
};

export const tool$repoListRepoPullrequests: ToolDefinition<typeof args> = {
  name: "repo-list-repo-pullrequests",
  description: `List all pullrequests`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await repoListRepoPullrequests(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
