/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ucGetUcComment } from "../../funcs/ucGetUcComment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUcCommentRequest$inboundSchema,
};

export const tool$ucGetUcComment: ToolDefinition<typeof args> = {
  name: "uc-get-uc-comment",
  description: `Retrieve a comment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ucGetUcComment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
