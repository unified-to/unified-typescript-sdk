/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { messageGetMessagingMessage } from "../../funcs/messageGetMessagingMessage.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetMessagingMessageRequest$inboundSchema,
};

export const tool$messageGetMessagingMessage: ToolDefinition<typeof args> = {
  name: "message-get-messaging-message",
  description: `Retrieve a message`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await messageGetMessagingMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
