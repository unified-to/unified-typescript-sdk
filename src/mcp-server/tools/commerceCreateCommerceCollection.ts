/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commerceCreateCommerceCollection } from "../../funcs/commerceCreateCommerceCollection.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCommerceCollectionRequest$inboundSchema,
};

export const tool$commerceCreateCommerceCollection: ToolDefinition<
  typeof args
> = {
  name: "commerce-create-commerce-collection",
  description: `Create a collection`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await commerceCreateCommerceCollection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
