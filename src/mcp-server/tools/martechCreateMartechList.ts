/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { martechCreateMartechList } from "../../funcs/martechCreateMartechList.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateMartechListRequest$inboundSchema,
};

export const tool$martechCreateMartechList: ToolDefinition<typeof args> = {
  name: "martech-create-martech-list",
  description: `Create a list`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await martechCreateMartechList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
