/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsPatchAtsJob } from "../../funcs/atsPatchAtsJob.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchAtsJobRequest$inboundSchema,
};

export const tool$atsPatchAtsJob: ToolDefinition<typeof args> = {
  name: "ats-patch-ats-job",
  description: `Update a job`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsPatchAtsJob(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
