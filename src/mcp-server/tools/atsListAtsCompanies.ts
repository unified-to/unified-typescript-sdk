/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsListAtsCompanies } from "../../funcs/atsListAtsCompanies.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAtsCompaniesRequest$inboundSchema,
};

export const tool$atsListAtsCompanies: ToolDefinition<typeof args> = {
  name: "ats-list-ats-companies",
  description: `List all companies`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsListAtsCompanies(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
