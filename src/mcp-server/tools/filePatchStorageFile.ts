/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { filePatchStorageFile } from "../../funcs/filePatchStorageFile.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchStorageFileRequest$inboundSchema,
};

export const tool$filePatchStorageFile: ToolDefinition<typeof args> = {
  name: "file-patch-storage-file",
  description: `Update a file`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await filePatchStorageFile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
