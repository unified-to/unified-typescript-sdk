/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ucPatchUcRecording } from "../../funcs/ucPatchUcRecording.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchUcRecordingRequest$inboundSchema,
};

export const tool$ucPatchUcRecording: ToolDefinition<typeof args> = {
  name: "uc-patch-uc-recording",
  description: `Update a recording`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ucPatchUcRecording(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
