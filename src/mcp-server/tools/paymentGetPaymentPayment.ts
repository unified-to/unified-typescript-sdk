/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentGetPaymentPayment } from "../../funcs/paymentGetPaymentPayment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPaymentPaymentRequest$inboundSchema,
};

export const tool$paymentGetPaymentPayment: ToolDefinition<typeof args> = {
  name: "payment-get-payment-payment",
  description: `Retrieve a payment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentGetPaymentPayment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
