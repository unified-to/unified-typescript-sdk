/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingUpdateAccountingInvoice } from "../../funcs/accountingUpdateAccountingInvoice.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAccountingInvoiceRequest$inboundSchema,
};

export const tool$accountingUpdateAccountingInvoice: ToolDefinition<
  typeof args
> = {
  name: "accounting-update-accounting-invoice",
  description: `Update an invoice`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingUpdateAccountingInvoice(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
