/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commerceListCommerceLocations } from "../../funcs/commerceListCommerceLocations.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCommerceLocationsRequest$inboundSchema,
};

export const tool$commerceListCommerceLocations: ToolDefinition<typeof args> = {
  name: "commerce-list-commerce-locations",
  description: `List all locations`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await commerceListCommerceLocations(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
