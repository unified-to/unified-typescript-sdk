/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingListAccountingBalancesheets } from "../../funcs/accountingListAccountingBalancesheets.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingBalancesheetsRequest$inboundSchema,
};

export const tool$accountingListAccountingBalancesheets: ToolDefinition<
  typeof args
> = {
  name: "accounting-list-accounting-balancesheets",
  description: `List all balancesheets`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingListAccountingBalancesheets(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
