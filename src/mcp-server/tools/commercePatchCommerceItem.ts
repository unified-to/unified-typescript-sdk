/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commercePatchCommerceItem } from "../../funcs/commercePatchCommerceItem.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchCommerceItemRequest$inboundSchema,
};

export const tool$commercePatchCommerceItem: ToolDefinition<typeof args> = {
  name: "commerce-patch-commerce-item",
  description: `Update an item`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await commercePatchCommerceItem(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
