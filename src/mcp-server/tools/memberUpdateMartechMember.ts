/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { memberUpdateMartechMember } from "../../funcs/memberUpdateMartechMember.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateMartechMemberRequest$inboundSchema,
};

export const tool$memberUpdateMartechMember: ToolDefinition<typeof args> = {
  name: "member-update-martech-member",
  description: `Update a member`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await memberUpdateMartechMember(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
