/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeeGetHrisEmployee } from "../../funcs/employeeGetHrisEmployee.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetHrisEmployeeRequest$inboundSchema,
};

export const tool$employeeGetHrisEmployee: ToolDefinition<typeof args> = {
  name: "employee-get-hris-employee",
  description: `Retrieve an employee`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeeGetHrisEmployee(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
