/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingListTicketingNotes } from "../../funcs/ticketingListTicketingNotes.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListTicketingNotesRequest$inboundSchema,
};

export const tool$ticketingListTicketingNotes: ToolDefinition<typeof args> = {
  name: "ticketing-list-ticketing-notes",
  description: `List all notes`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingListTicketingNotes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
