/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { unifiedCreateUnifiedConnection } from "../../funcs/unifiedCreateUnifiedConnection.js";
import * as shared from "../../sdk/models/shared/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: shared.Connection$inboundSchema,
};

export const tool$unifiedCreateUnifiedConnection: ToolDefinition<typeof args> =
  {
    name: "unified-create-unified-connection",
    description: `Create connection

Used only to import existing customer credentials; use "Create connection indirectly" instead`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await unifiedCreateUnifiedConnection(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
