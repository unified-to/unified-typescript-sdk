/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lmsListLmsCourses } from "../../funcs/lmsListLmsCourses.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListLmsCoursesRequest$inboundSchema,
};

export const tool$lmsListLmsCourses: ToolDefinition<typeof args> = {
  name: "lms-list-lms-courses",
  description: `List all courses`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lmsListLmsCourses(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
