/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { reviewRemoveCommerceReview } from "../../funcs/reviewRemoveCommerceReview.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveCommerceReviewRequest$inboundSchema,
};

export const tool$reviewRemoveCommerceReview: ToolDefinition<typeof args> = {
  name: "review-remove-commerce-review",
  description: `Remove a review`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await reviewRemoveCommerceReview(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
