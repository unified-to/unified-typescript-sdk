/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { memberRemoveMartechMember } from "../../funcs/memberRemoveMartechMember.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveMartechMemberRequest$inboundSchema,
};

export const tool$memberRemoveMartechMember: ToolDefinition<typeof args> = {
  name: "member-remove-martech-member",
  description: `Remove a member`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await memberRemoveMartechMember(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
