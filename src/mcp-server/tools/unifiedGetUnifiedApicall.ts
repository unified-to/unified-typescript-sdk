/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { unifiedGetUnifiedApicall } from "../../funcs/unifiedGetUnifiedApicall.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUnifiedApicallRequest$inboundSchema,
};

export const tool$unifiedGetUnifiedApicall: ToolDefinition<typeof args> = {
  name: "unified-get-unified-apicall",
  description: `Retrieve specific API Call by its ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await unifiedGetUnifiedApicall(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
