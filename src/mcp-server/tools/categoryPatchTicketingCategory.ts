/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { categoryPatchTicketingCategory } from "../../funcs/categoryPatchTicketingCategory.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchTicketingCategoryRequest$inboundSchema,
};

export const tool$categoryPatchTicketingCategory: ToolDefinition<typeof args> =
  {
    name: "category-patch-ticketing-category",
    description: `Update a category`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await categoryPatchTicketingCategory(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
