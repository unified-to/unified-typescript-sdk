/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsCreateAtsJob } from "../../funcs/atsCreateAtsJob.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateAtsJobRequest$inboundSchema,
};

export const tool$atsCreateAtsJob: ToolDefinition<typeof args> = {
  name: "ats-create-ats-job",
  description: `Create a job`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsCreateAtsJob(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
