/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customerGetTicketingCustomer } from "../../funcs/customerGetTicketingCustomer.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTicketingCustomerRequest$inboundSchema,
};

export const tool$customerGetTicketingCustomer: ToolDefinition<typeof args> = {
  name: "customer-get-ticketing-customer",
  description: `Retrieve a customer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customerGetTicketingCustomer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
