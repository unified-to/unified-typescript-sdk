/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { journalListAccountingJournals } from "../../funcs/journalListAccountingJournals.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingJournalsRequest$inboundSchema,
};

export const tool$journalListAccountingJournals: ToolDefinition<typeof args> = {
  name: "journal-list-accounting-journals",
  description: `List all journals`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await journalListAccountingJournals(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
