/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmGetCrmCompany } from "../../funcs/crmGetCrmCompany.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCrmCompanyRequest$inboundSchema,
};

export const tool$crmGetCrmCompany: ToolDefinition<typeof args> = {
  name: "crm-get-crm-company",
  description: `Retrieve a company`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await crmGetCrmCompany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
