/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metadataCreateMetadataMetadata } from "../../funcs/metadataCreateMetadataMetadata.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateMetadataMetadataRequest$inboundSchema,
};

export const tool$metadataCreateMetadataMetadata: ToolDefinition<typeof args> =
  {
    name: "metadata-create-metadata-metadata",
    description: `Create a metadata`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await metadataCreateMetadataMetadata(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
