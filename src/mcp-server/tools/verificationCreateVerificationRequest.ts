/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { verificationCreateVerificationRequest } from "../../funcs/verificationCreateVerificationRequest.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateVerificationRequestRequest$inboundSchema,
};

export const tool$verificationCreateVerificationRequest: ToolDefinition<
  typeof args
> = {
  name: "verification-create-verification-request",
  description: `Create a request`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await verificationCreateVerificationRequest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
