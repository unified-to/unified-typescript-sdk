/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { messageCreateMessagingMessage } from "../../funcs/messageCreateMessagingMessage.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateMessagingMessageRequest$inboundSchema,
};

export const tool$messageCreateMessagingMessage: ToolDefinition<typeof args> = {
  name: "message-create-messaging-message",
  description: `Create a message`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await messageCreateMessagingMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
