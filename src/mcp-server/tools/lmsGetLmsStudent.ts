/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lmsGetLmsStudent } from "../../funcs/lmsGetLmsStudent.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetLmsStudentRequest$inboundSchema,
};

export const tool$lmsGetLmsStudent: ToolDefinition<typeof args> = {
  name: "lms-get-lms-student",
  description: `Retrieve a student`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lmsGetLmsStudent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
