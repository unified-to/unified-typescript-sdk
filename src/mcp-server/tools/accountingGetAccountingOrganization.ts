/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingGetAccountingOrganization } from "../../funcs/accountingGetAccountingOrganization.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingOrganizationRequest$inboundSchema,
};

export const tool$accountingGetAccountingOrganization: ToolDefinition<
  typeof args
> = {
  name: "accounting-get-accounting-organization",
  description: `Retrieve an organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingGetAccountingOrganization(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
