/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsRemoveAtsApplication } from "../../funcs/atsRemoveAtsApplication.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveAtsApplicationRequest$inboundSchema,
};

export const tool$atsRemoveAtsApplication: ToolDefinition<typeof args> = {
  name: "ats-remove-ats-application",
  description: `Remove an application`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsRemoveAtsApplication(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
