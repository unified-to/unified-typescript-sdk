/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsListAtsInterviews } from "../../funcs/atsListAtsInterviews.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAtsInterviewsRequest$inboundSchema,
};

export const tool$atsListAtsInterviews: ToolDefinition<typeof args> = {
  name: "ats-list-ats-interviews",
  description: `List all interviews`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsListAtsInterviews(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
