/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customerUpdateTicketingCustomer } from "../../funcs/customerUpdateTicketingCustomer.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateTicketingCustomerRequest$inboundSchema,
};

export const tool$customerUpdateTicketingCustomer: ToolDefinition<typeof args> =
  {
    name: "customer-update-ticketing-customer",
    description: `Update a customer`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await customerUpdateTicketingCustomer(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
