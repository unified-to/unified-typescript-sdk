/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { reviewUpdateCommerceReview } from "../../funcs/reviewUpdateCommerceReview.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateCommerceReviewRequest$inboundSchema,
};

export const tool$reviewUpdateCommerceReview: ToolDefinition<typeof args> = {
  name: "review-update-commerce-review",
  description: `Update a review`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await reviewUpdateCommerceReview(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
