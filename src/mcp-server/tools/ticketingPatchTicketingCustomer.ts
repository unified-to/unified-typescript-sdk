/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingPatchTicketingCustomer } from "../../funcs/ticketingPatchTicketingCustomer.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchTicketingCustomerRequest$inboundSchema,
};

export const tool$ticketingPatchTicketingCustomer: ToolDefinition<typeof args> =
  {
    name: "ticketing-patch-ticketing-customer",
    description: `Update a customer`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await ticketingPatchTicketingCustomer(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
