/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisGetHrisDevice } from "../../funcs/hrisGetHrisDevice.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetHrisDeviceRequest$inboundSchema,
};

export const tool$hrisGetHrisDevice: ToolDefinition<typeof args> = {
  name: "hris-get-hris-device",
  description: `Retrieve a device`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await hrisGetHrisDevice(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
