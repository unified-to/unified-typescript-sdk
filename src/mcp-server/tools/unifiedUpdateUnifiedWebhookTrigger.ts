/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { unifiedUpdateUnifiedWebhookTrigger } from "../../funcs/unifiedUpdateUnifiedWebhookTrigger.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateUnifiedWebhookTriggerRequest$inboundSchema,
};

export const tool$unifiedUpdateUnifiedWebhookTrigger: ToolDefinition<
  typeof args
> = {
  name: "unified-update-unified-webhook-trigger",
  description: `Trigger webhook`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await unifiedUpdateUnifiedWebhookTrigger(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
