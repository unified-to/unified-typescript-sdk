/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { martechRemoveMartechMember } from "../../funcs/martechRemoveMartechMember.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveMartechMemberRequest$inboundSchema,
};

export const tool$martechRemoveMartechMember: ToolDefinition<typeof args> = {
  name: "martech-remove-martech-member",
  description: `Remove a member`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await martechRemoveMartechMember(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
