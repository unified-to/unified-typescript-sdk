/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { jobGetAtsJob } from "../../funcs/jobGetAtsJob.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAtsJobRequest$inboundSchema,
};

export const tool$jobGetAtsJob: ToolDefinition<typeof args> = {
  name: "job-get-ats-job",
  description: `Retrieve a job`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await jobGetAtsJob(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
