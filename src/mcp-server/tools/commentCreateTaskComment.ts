/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commentCreateTaskComment } from "../../funcs/commentCreateTaskComment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateTaskCommentRequest$inboundSchema,
};

export const tool$commentCreateTaskComment: ToolDefinition<typeof args> = {
  name: "comment-create-task-comment",
  description: `Create a comment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await commentCreateTaskComment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
