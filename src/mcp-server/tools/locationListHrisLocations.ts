/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { locationListHrisLocations } from "../../funcs/locationListHrisLocations.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListHrisLocationsRequest$inboundSchema,
};

export const tool$locationListHrisLocations: ToolDefinition<typeof args> = {
  name: "location-list-hris-locations",
  description: `List all locations`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await locationListHrisLocations(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
