/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metadataListMetadataMetadatas } from "../../funcs/metadataListMetadataMetadatas.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListMetadataMetadatasRequest$inboundSchema,
};

export const tool$metadataListMetadataMetadatas: ToolDefinition<typeof args> = {
  name: "metadata-list-metadata-metadatas",
  description: `List all metadatas`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metadataListMetadataMetadatas(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
