/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commentListUcComments } from "../../funcs/commentListUcComments.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListUcCommentsRequest$inboundSchema,
};

export const tool$commentListUcComments: ToolDefinition<typeof args> = {
  name: "comment-list-uc-comments",
  description: `List all comments`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await commentListUcComments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
