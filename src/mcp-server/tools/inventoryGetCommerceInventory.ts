/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { inventoryGetCommerceInventory } from "../../funcs/inventoryGetCommerceInventory.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCommerceInventoryRequest$inboundSchema,
};

export const tool$inventoryGetCommerceInventory: ToolDefinition<typeof args> = {
  name: "inventory-get-commerce-inventory",
  description: `Retrieve an inventory`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await inventoryGetCommerceInventory(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
