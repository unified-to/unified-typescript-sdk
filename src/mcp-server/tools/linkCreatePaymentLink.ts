/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkCreatePaymentLink } from "../../funcs/linkCreatePaymentLink.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreatePaymentLinkRequest$inboundSchema,
};

export const tool$linkCreatePaymentLink: ToolDefinition<typeof args> = {
  name: "link-create-payment-link",
  description: `Create a link`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await linkCreatePaymentLink(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
