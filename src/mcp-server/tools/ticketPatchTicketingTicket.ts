/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketPatchTicketingTicket } from "../../funcs/ticketPatchTicketingTicket.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchTicketingTicketRequest$inboundSchema,
};

export const tool$ticketPatchTicketingTicket: ToolDefinition<typeof args> = {
  name: "ticket-patch-ticketing-ticket",
  description: `Update a ticket`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketPatchTicketingTicket(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
