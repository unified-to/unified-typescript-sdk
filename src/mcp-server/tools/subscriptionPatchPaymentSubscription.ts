/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionPatchPaymentSubscription } from "../../funcs/subscriptionPatchPaymentSubscription.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchPaymentSubscriptionRequest$inboundSchema,
};

export const tool$subscriptionPatchPaymentSubscription: ToolDefinition<
  typeof args
> = {
  name: "subscription-patch-payment-subscription",
  description: `Update a subscription`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await subscriptionPatchPaymentSubscription(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
