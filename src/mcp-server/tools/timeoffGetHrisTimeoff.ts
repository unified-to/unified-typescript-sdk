/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { timeoffGetHrisTimeoff } from "../../funcs/timeoffGetHrisTimeoff.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetHrisTimeoffRequest$inboundSchema,
};

export const tool$timeoffGetHrisTimeoff: ToolDefinition<typeof args> = {
  name: "timeoff-get-hris-timeoff",
  description: `Retrieve a timeoff`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await timeoffGetHrisTimeoff(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
