/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentPatchAtsDocument } from "../../funcs/documentPatchAtsDocument.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchAtsDocumentRequest$inboundSchema,
};

export const tool$documentPatchAtsDocument: ToolDefinition<typeof args> = {
  name: "document-patch-ats-document",
  description: `Update a document`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentPatchAtsDocument(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
