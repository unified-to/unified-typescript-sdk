/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { martechListMartechMembers } from "../../funcs/martechListMartechMembers.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListMartechMembersRequest$inboundSchema,
};

export const tool$martechListMartechMembers: ToolDefinition<typeof args> = {
  name: "martech-list-martech-members",
  description: `List all members`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await martechListMartechMembers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
