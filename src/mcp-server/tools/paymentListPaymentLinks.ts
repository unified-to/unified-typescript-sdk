/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentListPaymentLinks } from "../../funcs/paymentListPaymentLinks.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListPaymentLinksRequest$inboundSchema,
};

export const tool$paymentListPaymentLinks: ToolDefinition<typeof args> = {
  name: "payment-list-payment-links",
  description: `List all links`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentListPaymentLinks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
