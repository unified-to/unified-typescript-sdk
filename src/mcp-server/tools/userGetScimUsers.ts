/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userGetScimUsers } from "../../funcs/userGetScimUsers.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetScimUsersRequest$inboundSchema,
};

export const tool$userGetScimUsers: ToolDefinition<typeof args> = {
  name: "user-get-scim-users",
  description: `Get user`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await userGetScimUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
