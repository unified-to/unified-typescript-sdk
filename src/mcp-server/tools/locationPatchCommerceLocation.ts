/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { locationPatchCommerceLocation } from "../../funcs/locationPatchCommerceLocation.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchCommerceLocationRequest$inboundSchema,
};

export const tool$locationPatchCommerceLocation: ToolDefinition<typeof args> = {
  name: "location-patch-commerce-location",
  description: `Update a location`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await locationPatchCommerceLocation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
