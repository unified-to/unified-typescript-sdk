/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmListCrmEvents } from "../../funcs/crmListCrmEvents.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCrmEventsRequest$inboundSchema,
};

export const tool$crmListCrmEvents: ToolDefinition<typeof args> = {
  name: "crm-list-crm-events",
  description: `List all events`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await crmListCrmEvents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
