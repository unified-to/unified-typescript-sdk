/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { orderPatchAccountingOrder } from "../../funcs/orderPatchAccountingOrder.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchAccountingOrderRequest$inboundSchema,
};

export const tool$orderPatchAccountingOrder: ToolDefinition<typeof args> = {
  name: "order-patch-accounting-order",
  description: `Update an order`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await orderPatchAccountingOrder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
