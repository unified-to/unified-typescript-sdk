/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dealPatchCrmDeal } from "../../funcs/dealPatchCrmDeal.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchCrmDealRequest$inboundSchema,
};

export const tool$dealPatchCrmDeal: ToolDefinition<typeof args> = {
  name: "deal-patch-crm-deal",
  description: `Update a deal`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dealPatchCrmDeal(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
