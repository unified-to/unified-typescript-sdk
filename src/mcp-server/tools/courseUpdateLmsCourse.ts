/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { courseUpdateLmsCourse } from "../../funcs/courseUpdateLmsCourse.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateLmsCourseRequest$inboundSchema,
};

export const tool$courseUpdateLmsCourse: ToolDefinition<typeof args> = {
  name: "course-update-lms-course",
  description: `Update a course`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await courseUpdateLmsCourse(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
