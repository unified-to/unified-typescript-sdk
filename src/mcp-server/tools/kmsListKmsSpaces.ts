/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kmsListKmsSpaces } from "../../funcs/kmsListKmsSpaces.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListKmsSpacesRequest$inboundSchema,
};

export const tool$kmsListKmsSpaces: ToolDefinition<typeof args> = {
  name: "kms-list-kms-spaces",
  description: `List all spaces`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kmsListKmsSpaces(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
