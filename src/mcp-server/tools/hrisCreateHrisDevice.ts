/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisCreateHrisDevice } from "../../funcs/hrisCreateHrisDevice.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateHrisDeviceRequest$inboundSchema,
};

export const tool$hrisCreateHrisDevice: ToolDefinition<typeof args> = {
  name: "hris-create-hris-device",
  description: `Create a device`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await hrisCreateHrisDevice(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
