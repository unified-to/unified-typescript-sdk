/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingRemoveAccountingContact } from "../../funcs/accountingRemoveAccountingContact.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveAccountingContactRequest$inboundSchema,
};

export const tool$accountingRemoveAccountingContact: ToolDefinition<
  typeof args
> = {
  name: "accounting-remove-accounting-contact",
  description: `Remove a contact`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingRemoveAccountingContact(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
