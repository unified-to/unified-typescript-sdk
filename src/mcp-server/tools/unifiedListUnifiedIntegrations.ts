/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { unifiedListUnifiedIntegrations } from "../../funcs/unifiedListUnifiedIntegrations.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListUnifiedIntegrationsRequest$inboundSchema,
};

export const tool$unifiedListUnifiedIntegrations: ToolDefinition<typeof args> =
  {
    name: "unified-list-unified-integrations",
    description: `Returns all integrations`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await unifiedListUnifiedIntegrations(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
