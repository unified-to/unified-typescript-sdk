/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repoPatchRepoPullrequest } from "../../funcs/repoPatchRepoPullrequest.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchRepoPullrequestRequest$inboundSchema,
};

export const tool$repoPatchRepoPullrequest: ToolDefinition<typeof args> = {
  name: "repo-patch-repo-pullrequest",
  description: `Update a pullrequest`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await repoPatchRepoPullrequest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
