/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectCreateTaskProject } from "../../funcs/projectCreateTaskProject.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateTaskProjectRequest$inboundSchema,
};

export const tool$projectCreateTaskProject: ToolDefinition<typeof args> = {
  name: "project-create-task-project",
  description: `Create a project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectCreateTaskProject(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
