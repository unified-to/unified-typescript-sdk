/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kmsCreateKmsSpace } from "../../funcs/kmsCreateKmsSpace.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateKmsSpaceRequest$inboundSchema,
};

export const tool$kmsCreateKmsSpace: ToolDefinition<typeof args> = {
  name: "kms-create-kms-space",
  description: `Create a space`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kmsCreateKmsSpace(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
