/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { journalUpdateAccountingJournal } from "../../funcs/journalUpdateAccountingJournal.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAccountingJournalRequest$inboundSchema,
};

export const tool$journalUpdateAccountingJournal: ToolDefinition<typeof args> =
  {
    name: "journal-update-accounting-journal",
    description: `Update a journal`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await journalUpdateAccountingJournal(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
