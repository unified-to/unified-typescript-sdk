/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentPatchPaymentPayment } from "../../funcs/paymentPatchPaymentPayment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchPaymentPaymentRequest$inboundSchema,
};

export const tool$paymentPatchPaymentPayment: ToolDefinition<typeof args> = {
  name: "payment-patch-payment-payment",
  description: `Update a payment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentPatchPaymentPayment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
