/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingGetAccountingSalesorder } from "../../funcs/accountingGetAccountingSalesorder.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingSalesorderRequest$inboundSchema,
};

export const tool$accountingGetAccountingSalesorder: ToolDefinition<
  typeof args
> = {
  name: "accounting-get-accounting-salesorder",
  description: `Retrieve a salesorder`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingGetAccountingSalesorder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
