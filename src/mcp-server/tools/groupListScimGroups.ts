/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupListScimGroups } from "../../funcs/groupListScimGroups.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListScimGroupsRequest$inboundSchema,
};

export const tool$groupListScimGroups: ToolDefinition<typeof args> = {
  name: "group-list-scim-groups",
  description: `List groups`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupListScimGroups(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
