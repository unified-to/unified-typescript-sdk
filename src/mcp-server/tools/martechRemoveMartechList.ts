/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { martechRemoveMartechList } from "../../funcs/martechRemoveMartechList.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveMartechListRequest$inboundSchema,
};

export const tool$martechRemoveMartechList: ToolDefinition<typeof args> = {
  name: "martech-remove-martech-list",
  description: `Remove a list`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await martechRemoveMartechList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
