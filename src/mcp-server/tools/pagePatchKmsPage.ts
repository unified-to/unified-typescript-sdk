/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pagePatchKmsPage } from "../../funcs/pagePatchKmsPage.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchKmsPageRequest$inboundSchema,
};

export const tool$pagePatchKmsPage: ToolDefinition<typeof args> = {
  name: "page-patch-kms-page",
  description: `Update a page`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await pagePatchKmsPage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
