/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { enrichListEnrichPeople } from "../../funcs/enrichListEnrichPeople.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListEnrichPeopleRequest$inboundSchema,
};

export const tool$enrichListEnrichPeople: ToolDefinition<typeof args> = {
  name: "enrich-list-enrich-people",
  description: `Retrieve enrichment information for a person`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await enrichListEnrichPeople(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
