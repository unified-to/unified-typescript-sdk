/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupCreateHrisGroup } from "../../funcs/groupCreateHrisGroup.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateHrisGroupRequest$inboundSchema,
};

export const tool$groupCreateHrisGroup: ToolDefinition<typeof args> = {
  name: "group-create-hris-group",
  description: `Create a group`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupCreateHrisGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
