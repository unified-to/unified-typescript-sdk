/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { storageListStorageFiles } from "../../funcs/storageListStorageFiles.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListStorageFilesRequest$inboundSchema,
};

export const tool$storageListStorageFiles: ToolDefinition<typeof args> = {
  name: "storage-list-storage-files",
  description: `List all files`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await storageListStorageFiles(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
