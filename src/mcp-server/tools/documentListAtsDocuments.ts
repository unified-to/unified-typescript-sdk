/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentListAtsDocuments } from "../../funcs/documentListAtsDocuments.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAtsDocumentsRequest$inboundSchema,
};

export const tool$documentListAtsDocuments: ToolDefinition<typeof args> = {
  name: "document-list-ats-documents",
  description: `List all documents`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentListAtsDocuments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
