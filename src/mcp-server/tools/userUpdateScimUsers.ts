/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userUpdateScimUsers } from "../../funcs/userUpdateScimUsers.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateScimUsersRequest$inboundSchema,
};

export const tool$userUpdateScimUsers: ToolDefinition<typeof args> = {
  name: "user-update-scim-users",
  description: `Update user`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await userUpdateScimUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
