/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingCreateTicketingNote } from "../../funcs/ticketingCreateTicketingNote.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateTicketingNoteRequest$inboundSchema,
};

export const tool$ticketingCreateTicketingNote: ToolDefinition<typeof args> = {
  name: "ticketing-create-ticketing-note",
  description: `Create a note`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingCreateTicketingNote(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
