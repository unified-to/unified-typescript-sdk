/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ucListUcComments } from "../../funcs/ucListUcComments.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListUcCommentsRequest$inboundSchema,
};

export const tool$ucListUcComments: ToolDefinition<typeof args> = {
  name: "uc-list-uc-comments",
  description: `List all comments`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ucListUcComments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
