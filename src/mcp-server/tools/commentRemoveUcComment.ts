/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commentRemoveUcComment } from "../../funcs/commentRemoveUcComment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveUcCommentRequest$inboundSchema,
};

export const tool$commentRemoveUcComment: ToolDefinition<typeof args> = {
  name: "comment-remove-uc-comment",
  description: `Remove a comment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await commentRemoveUcComment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
