/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passthroughListPassthroughs } from "../../funcs/passthroughListPassthroughs.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListPassthroughsRequest$inboundSchema,
};

export const tool$passthroughListPassthroughs: ToolDefinition<typeof args> = {
  name: "passthrough-list-passthroughs",
  description: `Passthrough GET`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await passthroughListPassthroughs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value?.result;

    return formatResult(value, apiCall);
  },
};
