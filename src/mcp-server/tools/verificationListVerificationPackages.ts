/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { verificationListVerificationPackages } from "../../funcs/verificationListVerificationPackages.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListVerificationPackagesRequest$inboundSchema,
};

export const tool$verificationListVerificationPackages: ToolDefinition<
  typeof args
> = {
  name: "verification-list-verification-packages",
  description: `List all packages`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await verificationListVerificationPackages(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
