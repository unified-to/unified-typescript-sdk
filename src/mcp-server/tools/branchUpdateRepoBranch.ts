/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { branchUpdateRepoBranch } from "../../funcs/branchUpdateRepoBranch.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateRepoBranchRequest$inboundSchema,
};

export const tool$branchUpdateRepoBranch: ToolDefinition<typeof args> = {
  name: "branch-update-repo-branch",
  description: `Update a branch`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await branchUpdateRepoBranch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
