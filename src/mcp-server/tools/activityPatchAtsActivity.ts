/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { activityPatchAtsActivity } from "../../funcs/activityPatchAtsActivity.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchAtsActivityRequest$inboundSchema,
};

export const tool$activityPatchAtsActivity: ToolDefinition<typeof args> = {
  name: "activity-patch-ats-activity",
  description: `Update an activity`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await activityPatchAtsActivity(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
