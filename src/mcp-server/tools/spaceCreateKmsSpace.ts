/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { spaceCreateKmsSpace } from "../../funcs/spaceCreateKmsSpace.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateKmsSpaceRequest$inboundSchema,
};

export const tool$spaceCreateKmsSpace: ToolDefinition<typeof args> = {
  name: "space-create-kms-space",
  description: `Create a space`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await spaceCreateKmsSpace(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
