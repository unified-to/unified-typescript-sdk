/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { itemUpdateCommerceItem } from "../../funcs/itemUpdateCommerceItem.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateCommerceItemRequest$inboundSchema,
};

export const tool$itemUpdateCommerceItem: ToolDefinition<typeof args> = {
  name: "item-update-commerce-item",
  description: `Update an item`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await itemUpdateCommerceItem(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
