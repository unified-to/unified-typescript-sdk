/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commerceRemoveCommerceItem } from "../../funcs/commerceRemoveCommerceItem.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveCommerceItemRequest$inboundSchema,
};

export const tool$commerceRemoveCommerceItem: ToolDefinition<typeof args> = {
  name: "commerce-remove-commerce-item",
  description: `Remove an item`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await commerceRemoveCommerceItem(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
