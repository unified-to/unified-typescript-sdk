/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingListAccountingInvoices } from "../../funcs/accountingListAccountingInvoices.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingInvoicesRequest$inboundSchema,
};

export const tool$accountingListAccountingInvoices: ToolDefinition<
  typeof args
> = {
  name: "accounting-list-accounting-invoices",
  description: `List all invoices`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingListAccountingInvoices(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
