/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { recordingListUcRecordings } from "../../funcs/recordingListUcRecordings.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListUcRecordingsRequest$inboundSchema,
};

export const tool$recordingListUcRecordings: ToolDefinition<typeof args> = {
  name: "recording-list-uc-recordings",
  description: `List all recordings`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await recordingListUcRecordings(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
