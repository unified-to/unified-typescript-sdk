/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ucCreateUcRecording } from "../../funcs/ucCreateUcRecording.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateUcRecordingRequest$inboundSchema,
};

export const tool$ucCreateUcRecording: ToolDefinition<typeof args> = {
  name: "uc-create-uc-recording",
  description: `Create a recording`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ucCreateUcRecording(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
