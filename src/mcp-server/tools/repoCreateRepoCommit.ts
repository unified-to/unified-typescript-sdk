/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repoCreateRepoCommit } from "../../funcs/repoCreateRepoCommit.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateRepoCommitRequest$inboundSchema,
};

export const tool$repoCreateRepoCommit: ToolDefinition<typeof args> = {
  name: "repo-create-repo-commit",
  description: `Create a commit`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await repoCreateRepoCommit(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
