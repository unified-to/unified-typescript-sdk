/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contactUpdateAccountingContact } from "../../funcs/contactUpdateAccountingContact.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAccountingContactRequest$inboundSchema,
};

export const tool$contactUpdateAccountingContact: ToolDefinition<typeof args> =
  {
    name: "contact-update-accounting-contact",
    description: `Update a contact`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await contactUpdateAccountingContact(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
