/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { messagingGetMessagingMessage } from "../../funcs/messagingGetMessagingMessage.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetMessagingMessageRequest$inboundSchema,
};

export const tool$messagingGetMessagingMessage: ToolDefinition<typeof args> = {
  name: "messaging-get-messaging-message",
  description: `Retrieve a message`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await messagingGetMessagingMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
