/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingRemoveAccountingTransaction } from "../../funcs/accountingRemoveAccountingTransaction.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveAccountingTransactionRequest$inboundSchema,
};

export const tool$accountingRemoveAccountingTransaction: ToolDefinition<
  typeof args
> = {
  name: "accounting-remove-accounting-transaction",
  description: `Remove a transaction`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingRemoveAccountingTransaction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
