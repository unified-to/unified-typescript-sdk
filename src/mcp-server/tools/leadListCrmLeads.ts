/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { leadListCrmLeads } from "../../funcs/leadListCrmLeads.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCrmLeadsRequest$inboundSchema,
};

export const tool$leadListCrmLeads: ToolDefinition<typeof args> = {
  name: "lead-list-crm-leads",
  description: `List all leads`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await leadListCrmLeads(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
