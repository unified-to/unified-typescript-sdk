/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scorecardUpdateAtsScorecard } from "../../funcs/scorecardUpdateAtsScorecard.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAtsScorecardRequest$inboundSchema,
};

export const tool$scorecardUpdateAtsScorecard: ToolDefinition<typeof args> = {
  name: "scorecard-update-ats-scorecard",
  description: `Update a scorecard`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scorecardUpdateAtsScorecard(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
