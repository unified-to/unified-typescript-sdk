/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repoUpdateRepoRepository } from "../../funcs/repoUpdateRepoRepository.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateRepoRepositoryRequest$inboundSchema,
};

export const tool$repoUpdateRepoRepository: ToolDefinition<typeof args> = {
  name: "repo-update-repo-repository",
  description: `Update a repository`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await repoUpdateRepoRepository(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
