/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentPatchPaymentSubscription } from "../../funcs/paymentPatchPaymentSubscription.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchPaymentSubscriptionRequest$inboundSchema,
};

export const tool$paymentPatchPaymentSubscription: ToolDefinition<typeof args> =
  {
    name: "payment-patch-payment-subscription",
    description: `Update a subscription`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await paymentPatchPaymentSubscription(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
