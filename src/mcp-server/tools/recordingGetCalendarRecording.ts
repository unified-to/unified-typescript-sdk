/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { recordingGetCalendarRecording } from "../../funcs/recordingGetCalendarRecording.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCalendarRecordingRequest$inboundSchema,
};

export const tool$recordingGetCalendarRecording: ToolDefinition<typeof args> = {
  name: "recording-get-calendar-recording",
  description: `Retrieve a recording`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await recordingGetCalendarRecording(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
