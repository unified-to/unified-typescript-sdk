/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scimGetScimUsers } from "../../funcs/scimGetScimUsers.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetScimUsersRequest$inboundSchema,
};

export const tool$scimGetScimUsers: ToolDefinition<typeof args> = {
  name: "scim-get-scim-users",
  description: `Get user`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scimGetScimUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
