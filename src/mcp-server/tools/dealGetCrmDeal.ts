/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dealGetCrmDeal } from "../../funcs/dealGetCrmDeal.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCrmDealRequest$inboundSchema,
};

export const tool$dealGetCrmDeal: ToolDefinition<typeof args> = {
  name: "deal-get-crm-deal",
  description: `Retrieve a deal`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dealGetCrmDeal(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
