/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectionPatchCommerceCollection } from "../../funcs/collectionPatchCommerceCollection.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchCommerceCollectionRequest$inboundSchema,
};

export const tool$collectionPatchCommerceCollection: ToolDefinition<
  typeof args
> = {
  name: "collection-patch-commerce-collection",
  description: `Update a collection`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectionPatchCommerceCollection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
