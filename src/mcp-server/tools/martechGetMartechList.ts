/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { martechGetMartechList } from "../../funcs/martechGetMartechList.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetMartechListRequest$inboundSchema,
};

export const tool$martechGetMartechList: ToolDefinition<typeof args> = {
  name: "martech-get-martech-list",
  description: `Retrieve a list`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await martechGetMartechList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
