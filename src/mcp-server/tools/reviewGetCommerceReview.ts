/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { reviewGetCommerceReview } from "../../funcs/reviewGetCommerceReview.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCommerceReviewRequest$inboundSchema,
};

export const tool$reviewGetCommerceReview: ToolDefinition<typeof args> = {
  name: "review-get-commerce-review",
  description: `Retrieve a review`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await reviewGetCommerceReview(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
