/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pageCreateKmsPage } from "../../funcs/pageCreateKmsPage.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateKmsPageRequest$inboundSchema,
};

export const tool$pageCreateKmsPage: ToolDefinition<typeof args> = {
  name: "page-create-kms-page",
  description: `Create a page`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await pageCreateKmsPage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
