/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingCreateTicketingTicket } from "../../funcs/ticketingCreateTicketingTicket.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateTicketingTicketRequest$inboundSchema,
};

export const tool$ticketingCreateTicketingTicket: ToolDefinition<typeof args> =
  {
    name: "ticketing-create-ticketing-ticket",
    description: `Create a ticket`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await ticketingCreateTicketingTicket(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
