/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passthroughPatchPassthroughJson } from "../../funcs/passthroughPatchPassthroughJson.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchPassthroughJsonRequest$inboundSchema,
};

export const tool$passthroughPatchPassthroughJson: ToolDefinition<typeof args> =
  {
    name: "passthrough-patch-passthrough-json",
    description: `Passthrough PUT`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await passthroughPatchPassthroughJson(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value?.result;

      return formatResult(value, apiCall);
    },
  };
