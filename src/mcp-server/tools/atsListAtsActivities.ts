/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsListAtsActivities } from "../../funcs/atsListAtsActivities.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAtsActivitiesRequest$inboundSchema,
};

export const tool$atsListAtsActivities: ToolDefinition<typeof args> = {
  name: "ats-list-ats-activities",
  description: `List all activities`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsListAtsActivities(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
