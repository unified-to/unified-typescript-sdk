/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyRemoveCrmCompany } from "../../funcs/companyRemoveCrmCompany.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveCrmCompanyRequest$inboundSchema,
};

export const tool$companyRemoveCrmCompany: ToolDefinition<typeof args> = {
  name: "company-remove-crm-company",
  description: `Remove a company`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyRemoveCrmCompany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
