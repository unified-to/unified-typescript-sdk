/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classListLmsClasses } from "../../funcs/classListLmsClasses.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListLmsClassesRequest$inboundSchema,
};

export const tool$classListLmsClasses: ToolDefinition<typeof args> = {
  name: "class-list-lms-classes",
  description: `List all classes`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await classListLmsClasses(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
