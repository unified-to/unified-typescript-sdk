/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { journalRemoveAccountingJournal } from "../../funcs/journalRemoveAccountingJournal.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveAccountingJournalRequest$inboundSchema,
};

export const tool$journalRemoveAccountingJournal: ToolDefinition<typeof args> =
  {
    name: "journal-remove-accounting-journal",
    description: `Remove a journal`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await journalRemoveAccountingJournal(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
