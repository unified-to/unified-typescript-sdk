/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsListAtsJobs } from "../../funcs/atsListAtsJobs.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAtsJobsRequest$inboundSchema,
};

export const tool$atsListAtsJobs: ToolDefinition<typeof args> = {
  name: "ats-list-ats-jobs",
  description: `List all jobs`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsListAtsJobs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
