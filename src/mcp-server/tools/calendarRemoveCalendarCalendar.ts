/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { calendarRemoveCalendarCalendar } from "../../funcs/calendarRemoveCalendarCalendar.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveCalendarCalendarRequest$inboundSchema,
};

export const tool$calendarRemoveCalendarCalendar: ToolDefinition<typeof args> =
  {
    name: "calendar-remove-calendar-calendar",
    description: `Remove a calendar`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await calendarRemoveCalendarCalendar(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
