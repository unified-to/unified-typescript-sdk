/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { itemListCommerceItems } from "../../funcs/itemListCommerceItems.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCommerceItemsRequest$inboundSchema,
};

export const tool$itemListCommerceItems: ToolDefinition<typeof args> = {
  name: "item-list-commerce-items",
  description: `List all items`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await itemListCommerceItems(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
