/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { inventoryListCommerceInventories } from "../../funcs/inventoryListCommerceInventories.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCommerceInventoriesRequest$inboundSchema,
};

export const tool$inventoryListCommerceInventories: ToolDefinition<
  typeof args
> = {
  name: "inventory-list-commerce-inventories",
  description: `List all inventories`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await inventoryListCommerceInventories(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
