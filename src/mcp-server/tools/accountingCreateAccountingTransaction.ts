/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingCreateAccountingTransaction } from "../../funcs/accountingCreateAccountingTransaction.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateAccountingTransactionRequest$inboundSchema,
};

export const tool$accountingCreateAccountingTransaction: ToolDefinition<
  typeof args
> = {
  name: "accounting-create-accounting-transaction",
  description: `Create a transaction`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingCreateAccountingTransaction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
