/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentRemovePaymentPayment } from "../../funcs/paymentRemovePaymentPayment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemovePaymentPaymentRequest$inboundSchema,
};

export const tool$paymentRemovePaymentPayment: ToolDefinition<typeof args> = {
  name: "payment-remove-payment-payment",
  description: `Remove a payment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentRemovePaymentPayment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
