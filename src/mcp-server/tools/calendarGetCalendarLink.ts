/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { calendarGetCalendarLink } from "../../funcs/calendarGetCalendarLink.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCalendarLinkRequest$inboundSchema,
};

export const tool$calendarGetCalendarLink: ToolDefinition<typeof args> = {
  name: "calendar-get-calendar-link",
  description: `Retrieve a link`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await calendarGetCalendarLink(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
