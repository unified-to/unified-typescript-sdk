/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingListAccountingTaxrates } from "../../funcs/accountingListAccountingTaxrates.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingTaxratesRequest$inboundSchema,
};

export const tool$accountingListAccountingTaxrates: ToolDefinition<
  typeof args
> = {
  name: "accounting-list-accounting-taxrates",
  description: `List all taxrates`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingListAccountingTaxrates(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
