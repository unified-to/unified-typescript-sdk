/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userCreateScimUsers } from "../../funcs/userCreateScimUsers.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateScimUsersRequest$inboundSchema,
};

export const tool$userCreateScimUsers: ToolDefinition<typeof args> = {
  name: "user-create-scim-users",
  description: `Create user`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await userCreateScimUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
