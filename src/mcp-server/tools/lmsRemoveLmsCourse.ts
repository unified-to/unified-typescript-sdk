/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lmsRemoveLmsCourse } from "../../funcs/lmsRemoveLmsCourse.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveLmsCourseRequest$inboundSchema,
};

export const tool$lmsRemoveLmsCourse: ToolDefinition<typeof args> = {
  name: "lms-remove-lms-course",
  description: `Remove a course`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lmsRemoveLmsCourse(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
