/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectionListUnifiedConnections } from "../../funcs/connectionListUnifiedConnections.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListUnifiedConnectionsRequest$inboundSchema,
};

export const tool$connectionListUnifiedConnections: ToolDefinition<
  typeof args
> = {
  name: "connection-list-unified-connections",
  description: `List all connections`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await connectionListUnifiedConnections(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
