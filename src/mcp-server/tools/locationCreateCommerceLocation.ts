/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { locationCreateCommerceLocation } from "../../funcs/locationCreateCommerceLocation.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCommerceLocationRequest$inboundSchema,
};

export const tool$locationCreateCommerceLocation: ToolDefinition<typeof args> =
  {
    name: "location-create-commerce-location",
    description: `Create a location`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await locationCreateCommerceLocation(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
