/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { spaceGetKmsSpace } from "../../funcs/spaceGetKmsSpace.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetKmsSpaceRequest$inboundSchema,
};

export const tool$spaceGetKmsSpace: ToolDefinition<typeof args> = {
  name: "space-get-kms-space",
  description: `Retrieve a space`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await spaceGetKmsSpace(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
