/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metadataGetMetadataMetadata } from "../../funcs/metadataGetMetadataMetadata.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetMetadataMetadataRequest$inboundSchema,
};

export const tool$metadataGetMetadataMetadata: ToolDefinition<typeof args> = {
  name: "metadata-get-metadata-metadata",
  description: `Retrieve a metadata`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metadataGetMetadataMetadata(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
