/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taskListTaskTasks } from "../../funcs/taskListTaskTasks.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListTaskTasksRequest$inboundSchema,
};

export const tool$taskListTaskTasks: ToolDefinition<typeof args> = {
  name: "task-list-task-tasks",
  description: `List all tasks`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await taskListTaskTasks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
