/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmRemoveCrmDeal } from "../../funcs/crmRemoveCrmDeal.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveCrmDealRequest$inboundSchema,
};

export const tool$crmRemoveCrmDeal: ToolDefinition<typeof args> = {
  name: "crm-remove-crm-deal",
  description: `Remove a deal`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await crmRemoveCrmDeal(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
