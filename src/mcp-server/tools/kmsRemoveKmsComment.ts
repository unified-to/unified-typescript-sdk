/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kmsRemoveKmsComment } from "../../funcs/kmsRemoveKmsComment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveKmsCommentRequest$inboundSchema,
};

export const tool$kmsRemoveKmsComment: ToolDefinition<typeof args> = {
  name: "kms-remove-kms-comment",
  description: `Remove a comment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kmsRemoveKmsComment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
