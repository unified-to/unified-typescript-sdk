/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingRemoveAccountingSalesorder } from "../../funcs/accountingRemoveAccountingSalesorder.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveAccountingSalesorderRequest$inboundSchema,
};

export const tool$accountingRemoveAccountingSalesorder: ToolDefinition<
  typeof args
> = {
  name: "accounting-remove-accounting-salesorder",
  description: `Remove a salesorder`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingRemoveAccountingSalesorder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
