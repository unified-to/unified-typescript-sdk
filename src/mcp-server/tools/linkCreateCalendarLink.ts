/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkCreateCalendarLink } from "../../funcs/linkCreateCalendarLink.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCalendarLinkRequest$inboundSchema,
};

export const tool$linkCreateCalendarLink: ToolDefinition<typeof args> = {
  name: "link-create-calendar-link",
  description: `Create a link`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await linkCreateCalendarLink(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
