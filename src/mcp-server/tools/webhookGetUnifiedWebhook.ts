/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhookGetUnifiedWebhook } from "../../funcs/webhookGetUnifiedWebhook.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUnifiedWebhookRequest$inboundSchema,
};

export const tool$webhookGetUnifiedWebhook: ToolDefinition<typeof args> = {
  name: "webhook-get-unified-webhook",
  description: `Retrieve webhook by its ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhookGetUnifiedWebhook(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
