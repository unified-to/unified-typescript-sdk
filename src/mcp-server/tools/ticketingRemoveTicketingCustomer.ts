/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingRemoveTicketingCustomer } from "../../funcs/ticketingRemoveTicketingCustomer.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveTicketingCustomerRequest$inboundSchema,
};

export const tool$ticketingRemoveTicketingCustomer: ToolDefinition<
  typeof args
> = {
  name: "ticketing-remove-ticketing-customer",
  description: `Remove a customer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingRemoveTicketingCustomer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
