/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ucUpdateUcComment } from "../../funcs/ucUpdateUcComment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateUcCommentRequest$inboundSchema,
};

export const tool$ucUpdateUcComment: ToolDefinition<typeof args> = {
  name: "uc-update-uc-comment",
  description: `Update a comment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ucUpdateUcComment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
