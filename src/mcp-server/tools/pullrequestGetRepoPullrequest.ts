/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pullrequestGetRepoPullrequest } from "../../funcs/pullrequestGetRepoPullrequest.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetRepoPullrequestRequest$inboundSchema,
};

export const tool$pullrequestGetRepoPullrequest: ToolDefinition<typeof args> = {
  name: "pullrequest-get-repo-pullrequest",
  description: `Retrieve a pullrequest`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await pullrequestGetRepoPullrequest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
