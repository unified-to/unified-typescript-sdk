/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingListAccountingAccounts } from "../../funcs/accountingListAccountingAccounts.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingAccountsRequest$inboundSchema,
};

export const tool$accountingListAccountingAccounts: ToolDefinition<
  typeof args
> = {
  name: "accounting-list-accounting-accounts",
  description: `List all accounts`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingListAccountingAccounts(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
