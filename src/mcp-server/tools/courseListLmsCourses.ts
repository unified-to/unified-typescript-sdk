/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { courseListLmsCourses } from "../../funcs/courseListLmsCourses.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListLmsCoursesRequest$inboundSchema,
};

export const tool$courseListLmsCourses: ToolDefinition<typeof args> = {
  name: "course-list-lms-courses",
  description: `List all courses`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await courseListLmsCourses(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
