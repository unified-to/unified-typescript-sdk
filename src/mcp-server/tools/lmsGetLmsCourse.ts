/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lmsGetLmsCourse } from "../../funcs/lmsGetLmsCourse.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetLmsCourseRequest$inboundSchema,
};

export const tool$lmsGetLmsCourse: ToolDefinition<typeof args> = {
  name: "lms-get-lms-course",
  description: `Retrieve a course`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lmsGetLmsCourse(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
