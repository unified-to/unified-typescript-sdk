/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { calendarListCalendarCalendars } from "../../funcs/calendarListCalendarCalendars.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCalendarCalendarsRequest$inboundSchema,
};

export const tool$calendarListCalendarCalendars: ToolDefinition<typeof args> = {
  name: "calendar-list-calendar-calendars",
  description: `List all calendars`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await calendarListCalendarCalendars(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
