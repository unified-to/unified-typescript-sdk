/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commitUpdateRepoCommit } from "../../funcs/commitUpdateRepoCommit.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateRepoCommitRequest$inboundSchema,
};

export const tool$commitUpdateRepoCommit: ToolDefinition<typeof args> = {
  name: "commit-update-repo-commit",
  description: `Update a commit`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await commitUpdateRepoCommit(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
