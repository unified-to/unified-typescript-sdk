/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kmsRemoveKmsPage } from "../../funcs/kmsRemoveKmsPage.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveKmsPageRequest$inboundSchema,
};

export const tool$kmsRemoveKmsPage: ToolDefinition<typeof args> = {
  name: "kms-remove-kms-page",
  description: `Remove a page`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kmsRemoveKmsPage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
