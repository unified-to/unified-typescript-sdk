/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupGetScimGroups } from "../../funcs/groupGetScimGroups.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetScimGroupsRequest$inboundSchema,
};

export const tool$groupGetScimGroups: ToolDefinition<typeof args> = {
  name: "group-get-scim-groups",
  description: `Get group`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupGetScimGroups(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
