/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repositoryListRepoRepositories } from "../../funcs/repositoryListRepoRepositories.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListRepoRepositoriesRequest$inboundSchema,
};

export const tool$repositoryListRepoRepositories: ToolDefinition<typeof args> =
  {
    name: "repository-list-repo-repositories",
    description: `List all repositories`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await repositoryListRepoRepositories(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
