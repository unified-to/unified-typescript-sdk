/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingListAccountingTrialbalances } from "../../funcs/accountingListAccountingTrialbalances.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingTrialbalancesRequest$inboundSchema,
};

export const tool$accountingListAccountingTrialbalances: ToolDefinition<
  typeof args
> = {
  name: "accounting-list-accounting-trialbalances",
  description: `List all trialbalances`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingListAccountingTrialbalances(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
