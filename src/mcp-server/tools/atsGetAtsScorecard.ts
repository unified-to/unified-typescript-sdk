/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsGetAtsScorecard } from "../../funcs/atsGetAtsScorecard.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAtsScorecardRequest$inboundSchema,
};

export const tool$atsGetAtsScorecard: ToolDefinition<typeof args> = {
  name: "ats-get-ats-scorecard",
  description: `Retrieve a scorecard`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsGetAtsScorecard(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
