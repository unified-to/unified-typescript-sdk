/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repoUpdateRepoCommit } from "../../funcs/repoUpdateRepoCommit.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateRepoCommitRequest$inboundSchema,
};

export const tool$repoUpdateRepoCommit: ToolDefinition<typeof args> = {
  name: "repo-update-repo-commit",
  description: `Update a commit`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await repoUpdateRepoCommit(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
