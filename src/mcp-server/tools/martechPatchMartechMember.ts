/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { martechPatchMartechMember } from "../../funcs/martechPatchMartechMember.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchMartechMemberRequest$inboundSchema,
};

export const tool$martechPatchMartechMember: ToolDefinition<typeof args> = {
  name: "martech-patch-martech-member",
  description: `Update a member`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await martechPatchMartechMember(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
