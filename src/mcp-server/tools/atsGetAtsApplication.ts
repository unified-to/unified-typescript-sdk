/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsGetAtsApplication } from "../../funcs/atsGetAtsApplication.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAtsApplicationRequest$inboundSchema,
};

export const tool$atsGetAtsApplication: ToolDefinition<typeof args> = {
  name: "ats-get-ats-application",
  description: `Retrieve an application`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsGetAtsApplication(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
