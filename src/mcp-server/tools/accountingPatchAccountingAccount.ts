/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingPatchAccountingAccount } from "../../funcs/accountingPatchAccountingAccount.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchAccountingAccountRequest$inboundSchema,
};

export const tool$accountingPatchAccountingAccount: ToolDefinition<
  typeof args
> = {
  name: "accounting-patch-accounting-account",
  description: `Update an account`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingPatchAccountingAccount(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
