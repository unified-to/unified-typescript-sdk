/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kmsGetKmsComment } from "../../funcs/kmsGetKmsComment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetKmsCommentRequest$inboundSchema,
};

export const tool$kmsGetKmsComment: ToolDefinition<typeof args> = {
  name: "kms-get-kms-comment",
  description: `Retrieve a comment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kmsGetKmsComment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
