/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lmsCreateLmsInstructor } from "../../funcs/lmsCreateLmsInstructor.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateLmsInstructorRequest$inboundSchema,
};

export const tool$lmsCreateLmsInstructor: ToolDefinition<typeof args> = {
  name: "lms-create-lms-instructor",
  description: `Create an instructor`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lmsCreateLmsInstructor(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
