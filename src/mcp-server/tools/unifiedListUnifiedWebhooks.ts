/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { unifiedListUnifiedWebhooks } from "../../funcs/unifiedListUnifiedWebhooks.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListUnifiedWebhooksRequest$inboundSchema,
};

export const tool$unifiedListUnifiedWebhooks: ToolDefinition<typeof args> = {
  name: "unified-list-unified-webhooks",
  description: `Returns all registered webhooks`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await unifiedListUnifiedWebhooks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
