/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fileCreateStorageFile } from "../../funcs/fileCreateStorageFile.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateStorageFileRequest$inboundSchema,
};

export const tool$fileCreateStorageFile: ToolDefinition<typeof args> = {
  name: "file-create-storage-file",
  description: `Create a file`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await fileCreateStorageFile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
