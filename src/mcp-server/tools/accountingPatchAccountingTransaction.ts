/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingPatchAccountingTransaction } from "../../funcs/accountingPatchAccountingTransaction.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchAccountingTransactionRequest$inboundSchema,
};

export const tool$accountingPatchAccountingTransaction: ToolDefinition<
  typeof args
> = {
  name: "accounting-patch-accounting-transaction",
  description: `Update a transaction`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingPatchAccountingTransaction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
