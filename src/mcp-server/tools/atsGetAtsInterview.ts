/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsGetAtsInterview } from "../../funcs/atsGetAtsInterview.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAtsInterviewRequest$inboundSchema,
};

export const tool$atsGetAtsInterview: ToolDefinition<typeof args> = {
  name: "ats-get-ats-interview",
  description: `Retrieve an interview`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsGetAtsInterview(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
