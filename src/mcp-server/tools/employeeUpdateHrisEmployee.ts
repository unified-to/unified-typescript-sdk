/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { employeeUpdateHrisEmployee } from "../../funcs/employeeUpdateHrisEmployee.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateHrisEmployeeRequest$inboundSchema,
};

export const tool$employeeUpdateHrisEmployee: ToolDefinition<typeof args> = {
  name: "employee-update-hris-employee",
  description: `Update an employee`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await employeeUpdateHrisEmployee(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
