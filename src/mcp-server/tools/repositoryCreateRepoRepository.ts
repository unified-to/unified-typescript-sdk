/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repositoryCreateRepoRepository } from "../../funcs/repositoryCreateRepoRepository.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateRepoRepositoryRequest$inboundSchema,
};

export const tool$repositoryCreateRepoRepository: ToolDefinition<typeof args> =
  {
    name: "repository-create-repo-repository",
    description: `Create a repository`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await repositoryCreateRepoRepository(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
