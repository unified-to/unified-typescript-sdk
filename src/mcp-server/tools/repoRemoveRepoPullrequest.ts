/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repoRemoveRepoPullrequest } from "../../funcs/repoRemoveRepoPullrequest.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveRepoPullrequestRequest$inboundSchema,
};

export const tool$repoRemoveRepoPullrequest: ToolDefinition<typeof args> = {
  name: "repo-remove-repo-pullrequest",
  description: `Remove a pullrequest`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await repoRemoveRepoPullrequest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
