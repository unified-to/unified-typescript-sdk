/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { studentListLmsStudents } from "../../funcs/studentListLmsStudents.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListLmsStudentsRequest$inboundSchema,
};

export const tool$studentListLmsStudents: ToolDefinition<typeof args> = {
  name: "student-list-lms-students",
  description: `List all students`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await studentListLmsStudents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
