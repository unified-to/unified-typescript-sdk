/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { recordingGetUcRecording } from "../../funcs/recordingGetUcRecording.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUcRecordingRequest$inboundSchema,
};

export const tool$recordingGetUcRecording: ToolDefinition<typeof args> = {
  name: "recording-get-uc-recording",
  description: `Retrieve a recording`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await recordingGetUcRecording(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
