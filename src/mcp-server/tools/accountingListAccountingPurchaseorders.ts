/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingListAccountingPurchaseorders } from "../../funcs/accountingListAccountingPurchaseorders.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingPurchaseordersRequest$inboundSchema,
};

export const tool$accountingListAccountingPurchaseorders: ToolDefinition<
  typeof args
> = {
  name: "accounting-list-accounting-purchaseorders",
  description: `List all purchaseorders`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingListAccountingPurchaseorders(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
