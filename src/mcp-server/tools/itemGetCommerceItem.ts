/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { itemGetCommerceItem } from "../../funcs/itemGetCommerceItem.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCommerceItemRequest$inboundSchema,
};

export const tool$itemGetCommerceItem: ToolDefinition<typeof args> = {
  name: "item-get-commerce-item",
  description: `Retrieve an item`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await itemGetCommerceItem(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
