/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { listCreateMartechList } from "../../funcs/listCreateMartechList.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateMartechListRequest$inboundSchema,
};

export const tool$listCreateMartechList: ToolDefinition<typeof args> = {
  name: "list-create-martech-list",
  description: `Create a list`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await listCreateMartechList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
