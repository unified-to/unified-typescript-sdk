/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsPatchAtsScorecard } from "../../funcs/atsPatchAtsScorecard.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchAtsScorecardRequest$inboundSchema,
};

export const tool$atsPatchAtsScorecard: ToolDefinition<typeof args> = {
  name: "ats-patch-ats-scorecard",
  description: `Update a scorecard`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsPatchAtsScorecard(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
