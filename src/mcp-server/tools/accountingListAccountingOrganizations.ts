/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingListAccountingOrganizations } from "../../funcs/accountingListAccountingOrganizations.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingOrganizationsRequest$inboundSchema,
};

export const tool$accountingListAccountingOrganizations: ToolDefinition<
  typeof args
> = {
  name: "accounting-list-accounting-organizations",
  description: `List all organizations`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingListAccountingOrganizations(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
