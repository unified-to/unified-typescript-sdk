/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { recordingUpdateUcRecording } from "../../funcs/recordingUpdateUcRecording.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateUcRecordingRequest$inboundSchema,
};

export const tool$recordingUpdateUcRecording: ToolDefinition<typeof args> = {
  name: "recording-update-uc-recording",
  description: `Update a recording`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await recordingUpdateUcRecording(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
