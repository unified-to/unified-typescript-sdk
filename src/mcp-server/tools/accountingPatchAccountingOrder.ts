/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingPatchAccountingOrder } from "../../funcs/accountingPatchAccountingOrder.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchAccountingOrderRequest$inboundSchema,
};

export const tool$accountingPatchAccountingOrder: ToolDefinition<typeof args> =
  {
    name: "accounting-patch-accounting-order",
    description: `Update an order`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await accountingPatchAccountingOrder(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
