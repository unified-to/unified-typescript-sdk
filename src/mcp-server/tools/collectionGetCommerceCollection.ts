/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectionGetCommerceCollection } from "../../funcs/collectionGetCommerceCollection.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCommerceCollectionRequest$inboundSchema,
};

export const tool$collectionGetCommerceCollection: ToolDefinition<typeof args> =
  {
    name: "collection-get-commerce-collection",
    description: `Retrieve a collection`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await collectionGetCommerceCollection(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
