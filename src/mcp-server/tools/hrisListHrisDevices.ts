/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisListHrisDevices } from "../../funcs/hrisListHrisDevices.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListHrisDevicesRequest$inboundSchema,
};

export const tool$hrisListHrisDevices: ToolDefinition<typeof args> = {
  name: "hris-list-hris-devices",
  description: `List all devices`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await hrisListHrisDevices(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
