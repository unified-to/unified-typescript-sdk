/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lmsListLmsStudents } from "../../funcs/lmsListLmsStudents.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListLmsStudentsRequest$inboundSchema,
};

export const tool$lmsListLmsStudents: ToolDefinition<typeof args> = {
  name: "lms-list-lms-students",
  description: `List all students`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lmsListLmsStudents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
