/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contactRemoveUcContact } from "../../funcs/contactRemoveUcContact.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveUcContactRequest$inboundSchema,
};

export const tool$contactRemoveUcContact: ToolDefinition<typeof args> = {
  name: "contact-remove-uc-contact",
  description: `Remove a contact`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contactRemoveUcContact(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
