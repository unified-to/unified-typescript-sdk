/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisRemoveHrisCompany } from "../../funcs/hrisRemoveHrisCompany.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveHrisCompanyRequest$inboundSchema,
};

export const tool$hrisRemoveHrisCompany: ToolDefinition<typeof args> = {
  name: "hris-remove-hris-company",
  description: `Remove a company`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await hrisRemoveHrisCompany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
