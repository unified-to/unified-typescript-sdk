/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { candidateListAtsCandidates } from "../../funcs/candidateListAtsCandidates.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAtsCandidatesRequest$inboundSchema,
};

export const tool$candidateListAtsCandidates: ToolDefinition<typeof args> = {
  name: "candidate-list-ats-candidates",
  description: `List all candidates`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await candidateListAtsCandidates(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
