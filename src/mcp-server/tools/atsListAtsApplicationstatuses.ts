/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsListAtsApplicationstatuses } from "../../funcs/atsListAtsApplicationstatuses.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAtsApplicationstatusesRequest$inboundSchema,
};

export const tool$atsListAtsApplicationstatuses: ToolDefinition<typeof args> = {
  name: "ats-list-ats-applicationstatuses",
  description: `List all applicationstatuses`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsListAtsApplicationstatuses(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
