/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmPatchCrmEvent } from "../../funcs/crmPatchCrmEvent.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchCrmEventRequest$inboundSchema,
};

export const tool$crmPatchCrmEvent: ToolDefinition<typeof args> = {
  name: "crm-patch-crm-event",
  description: `Update an event`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await crmPatchCrmEvent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
