/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsCreateAtsApplication } from "../../funcs/atsCreateAtsApplication.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateAtsApplicationRequest$inboundSchema,
};

export const tool$atsCreateAtsApplication: ToolDefinition<typeof args> = {
  name: "ats-create-ats-application",
  description: `Create an application`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsCreateAtsApplication(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
