/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commentRemoveTaskComment } from "../../funcs/commentRemoveTaskComment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveTaskCommentRequest$inboundSchema,
};

export const tool$commentRemoveTaskComment: ToolDefinition<typeof args> = {
  name: "comment-remove-task-comment",
  description: `Remove a comment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await commentRemoveTaskComment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
