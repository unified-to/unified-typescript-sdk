/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { messagePatchMessagingMessage } from "../../funcs/messagePatchMessagingMessage.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchMessagingMessageRequest$inboundSchema,
};

export const tool$messagePatchMessagingMessage: ToolDefinition<typeof args> = {
  name: "message-patch-messaging-message",
  description: `Update a message`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await messagePatchMessagingMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
