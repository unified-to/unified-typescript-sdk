/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkListCalendarLinks } from "../../funcs/linkListCalendarLinks.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCalendarLinksRequest$inboundSchema,
};

export const tool$linkListCalendarLinks: ToolDefinition<typeof args> = {
  name: "link-list-calendar-links",
  description: `List all links`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await linkListCalendarLinks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
