/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { orderListAccountingOrders } from "../../funcs/orderListAccountingOrders.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAccountingOrdersRequest$inboundSchema,
};

export const tool$orderListAccountingOrders: ToolDefinition<typeof args> = {
  name: "order-list-accounting-orders",
  description: `List all orders`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await orderListAccountingOrders(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
