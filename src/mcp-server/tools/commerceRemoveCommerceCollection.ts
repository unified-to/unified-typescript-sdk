/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commerceRemoveCommerceCollection } from "../../funcs/commerceRemoveCommerceCollection.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveCommerceCollectionRequest$inboundSchema,
};

export const tool$commerceRemoveCommerceCollection: ToolDefinition<
  typeof args
> = {
  name: "commerce-remove-commerce-collection",
  description: `Remove a collection`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await commerceRemoveCommerceCollection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
