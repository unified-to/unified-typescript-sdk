/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kmsUpdateKmsSpace } from "../../funcs/kmsUpdateKmsSpace.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateKmsSpaceRequest$inboundSchema,
};

export const tool$kmsUpdateKmsSpace: ToolDefinition<typeof args> = {
  name: "kms-update-kms-space",
  description: `Update a space`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kmsUpdateKmsSpace(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
