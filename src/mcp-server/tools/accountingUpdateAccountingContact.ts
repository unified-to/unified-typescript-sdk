/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingUpdateAccountingContact } from "../../funcs/accountingUpdateAccountingContact.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAccountingContactRequest$inboundSchema,
};

export const tool$accountingUpdateAccountingContact: ToolDefinition<
  typeof args
> = {
  name: "accounting-update-accounting-contact",
  description: `Update a contact`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingUpdateAccountingContact(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
