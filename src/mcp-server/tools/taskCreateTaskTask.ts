/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taskCreateTaskTask } from "../../funcs/taskCreateTaskTask.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateTaskTaskRequest$inboundSchema,
};

export const tool$taskCreateTaskTask: ToolDefinition<typeof args> = {
  name: "task-create-task-task",
  description: `Create a task`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await taskCreateTaskTask(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
