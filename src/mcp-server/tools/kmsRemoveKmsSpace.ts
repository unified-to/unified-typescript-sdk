/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kmsRemoveKmsSpace } from "../../funcs/kmsRemoveKmsSpace.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveKmsSpaceRequest$inboundSchema,
};

export const tool$kmsRemoveKmsSpace: ToolDefinition<typeof args> = {
  name: "kms-remove-kms-space",
  description: `Remove a space`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kmsRemoveKmsSpace(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
