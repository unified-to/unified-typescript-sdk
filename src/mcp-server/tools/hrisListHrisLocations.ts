/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisListHrisLocations } from "../../funcs/hrisListHrisLocations.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListHrisLocationsRequest$inboundSchema,
};

export const tool$hrisListHrisLocations: ToolDefinition<typeof args> = {
  name: "hris-list-hris-locations",
  description: `List all locations`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await hrisListHrisLocations(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
