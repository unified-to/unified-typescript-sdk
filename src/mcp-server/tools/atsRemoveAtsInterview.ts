/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsRemoveAtsInterview } from "../../funcs/atsRemoveAtsInterview.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveAtsInterviewRequest$inboundSchema,
};

export const tool$atsRemoveAtsInterview: ToolDefinition<typeof args> = {
  name: "ats-remove-ats-interview",
  description: `Remove an interview`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsRemoveAtsInterview(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
