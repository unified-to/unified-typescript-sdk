/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scorecardCreateAtsScorecard } from "../../funcs/scorecardCreateAtsScorecard.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateAtsScorecardRequest$inboundSchema,
};

export const tool$scorecardCreateAtsScorecard: ToolDefinition<typeof args> = {
  name: "scorecard-create-ats-scorecard",
  description: `Create a scorecard`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scorecardCreateAtsScorecard(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
