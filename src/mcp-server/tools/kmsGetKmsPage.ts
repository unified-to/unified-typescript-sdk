/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kmsGetKmsPage } from "../../funcs/kmsGetKmsPage.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetKmsPageRequest$inboundSchema,
};

export const tool$kmsGetKmsPage: ToolDefinition<typeof args> = {
  name: "kms-get-kms-page",
  description: `Retrieve a page`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kmsGetKmsPage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
