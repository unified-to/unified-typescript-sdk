/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountCreateAccountingAccount } from "../../funcs/accountCreateAccountingAccount.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateAccountingAccountRequest$inboundSchema,
};

export const tool$accountCreateAccountingAccount: ToolDefinition<typeof args> =
  {
    name: "account-create-accounting-account",
    description: `Create an account`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await accountCreateAccountingAccount(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
