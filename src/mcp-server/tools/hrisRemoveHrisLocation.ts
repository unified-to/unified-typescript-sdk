/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisRemoveHrisLocation } from "../../funcs/hrisRemoveHrisLocation.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveHrisLocationRequest$inboundSchema,
};

export const tool$hrisRemoveHrisLocation: ToolDefinition<typeof args> = {
  name: "hris-remove-hris-location",
  description: `Remove a location`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await hrisRemoveHrisLocation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
