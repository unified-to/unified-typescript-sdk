/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectionListCommerceCollections } from "../../funcs/collectionListCommerceCollections.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCommerceCollectionsRequest$inboundSchema,
};

export const tool$collectionListCommerceCollections: ToolDefinition<
  typeof args
> = {
  name: "collection-list-commerce-collections",
  description: `List all collections`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectionListCommerceCollections(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
