/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingRemoveAccountingBill } from "../../funcs/accountingRemoveAccountingBill.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveAccountingBillRequest$inboundSchema,
};

export const tool$accountingRemoveAccountingBill: ToolDefinition<typeof args> =
  {
    name: "accounting-remove-accounting-bill",
    description: `Remove a bill`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await accountingRemoveAccountingBill(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
