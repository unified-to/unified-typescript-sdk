/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commerceRemoveCommerceInventory } from "../../funcs/commerceRemoveCommerceInventory.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveCommerceInventoryRequest$inboundSchema,
};

export const tool$commerceRemoveCommerceInventory: ToolDefinition<typeof args> =
  {
    name: "commerce-remove-commerce-inventory",
    description: `Remove an inventory`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await commerceRemoveCommerceInventory(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
