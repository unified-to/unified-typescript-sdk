/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invoiceCreateAccountingInvoice } from "../../funcs/invoiceCreateAccountingInvoice.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateAccountingInvoiceRequest$inboundSchema,
};

export const tool$invoiceCreateAccountingInvoice: ToolDefinition<typeof args> =
  {
    name: "invoice-create-accounting-invoice",
    description: `Create an invoice`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await invoiceCreateAccountingInvoice(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
