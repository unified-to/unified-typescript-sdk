/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisGetHrisTimeoff } from "../../funcs/hrisGetHrisTimeoff.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetHrisTimeoffRequest$inboundSchema,
};

export const tool$hrisGetHrisTimeoff: ToolDefinition<typeof args> = {
  name: "hris-get-hris-timeoff",
  description: `Retrieve a timeoff`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await hrisGetHrisTimeoff(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
