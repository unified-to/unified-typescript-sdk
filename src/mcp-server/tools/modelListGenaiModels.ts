/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { modelListGenaiModels } from "../../funcs/modelListGenaiModels.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListGenaiModelsRequest$inboundSchema,
};

export const tool$modelListGenaiModels: ToolDefinition<typeof args> = {
  name: "model-list-genai-models",
  description: `List all models`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await modelListGenaiModels(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
