/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lmsCreateLmsClass } from "../../funcs/lmsCreateLmsClass.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateLmsClassRequest$inboundSchema,
};

export const tool$lmsCreateLmsClass: ToolDefinition<typeof args> = {
  name: "lms-create-lms-class",
  description: `Create a class`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lmsCreateLmsClass(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
