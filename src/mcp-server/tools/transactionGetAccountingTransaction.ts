/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transactionGetAccountingTransaction } from "../../funcs/transactionGetAccountingTransaction.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingTransactionRequest$inboundSchema,
};

export const tool$transactionGetAccountingTransaction: ToolDefinition<
  typeof args
> = {
  name: "transaction-get-accounting-transaction",
  description: `Retrieve a transaction`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transactionGetAccountingTransaction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
