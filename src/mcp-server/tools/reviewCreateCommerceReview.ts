/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { reviewCreateCommerceReview } from "../../funcs/reviewCreateCommerceReview.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCommerceReviewRequest$inboundSchema,
};

export const tool$reviewCreateCommerceReview: ToolDefinition<typeof args> = {
  name: "review-create-commerce-review",
  description: `Create a review`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await reviewCreateCommerceReview(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
