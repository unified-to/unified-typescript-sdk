/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyRemoveHrisCompany } from "../../funcs/companyRemoveHrisCompany.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveHrisCompanyRequest$inboundSchema,
};

export const tool$companyRemoveHrisCompany: ToolDefinition<typeof args> = {
  name: "company-remove-hris-company",
  description: `Remove a company`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyRemoveHrisCompany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
