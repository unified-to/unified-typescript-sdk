/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { messagingListMessagingMessages } from "../../funcs/messagingListMessagingMessages.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListMessagingMessagesRequest$inboundSchema,
};

export const tool$messagingListMessagingMessages: ToolDefinition<typeof args> =
  {
    name: "messaging-list-messaging-messages",
    description: `List all messages`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await messagingListMessagingMessages(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
