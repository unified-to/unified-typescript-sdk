/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ucGetUcContact } from "../../funcs/ucGetUcContact.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUcContactRequest$inboundSchema,
};

export const tool$ucGetUcContact: ToolDefinition<typeof args> = {
  name: "uc-get-uc-contact",
  description: `Retrieve a contact`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ucGetUcContact(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
