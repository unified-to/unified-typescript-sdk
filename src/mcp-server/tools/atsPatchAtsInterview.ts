/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsPatchAtsInterview } from "../../funcs/atsPatchAtsInterview.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchAtsInterviewRequest$inboundSchema,
};

export const tool$atsPatchAtsInterview: ToolDefinition<typeof args> = {
  name: "ats-patch-ats-interview",
  description: `Update an interview`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await atsPatchAtsInterview(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
