/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupPatchScimGroups } from "../../funcs/groupPatchScimGroups.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchScimGroupsRequest$inboundSchema,
};

export const tool$groupPatchScimGroups: ToolDefinition<typeof args> = {
  name: "group-patch-scim-groups",
  description: `Update group`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await groupPatchScimGroups(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
