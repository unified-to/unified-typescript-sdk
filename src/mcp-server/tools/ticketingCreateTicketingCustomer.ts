/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingCreateTicketingCustomer } from "../../funcs/ticketingCreateTicketingCustomer.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateTicketingCustomerRequest$inboundSchema,
};

export const tool$ticketingCreateTicketingCustomer: ToolDefinition<
  typeof args
> = {
  name: "ticketing-create-ticketing-customer",
  description: `Create a customer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingCreateTicketingCustomer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
