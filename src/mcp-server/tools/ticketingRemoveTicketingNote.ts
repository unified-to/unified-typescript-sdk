/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingRemoveTicketingNote } from "../../funcs/ticketingRemoveTicketingNote.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveTicketingNoteRequest$inboundSchema,
};

export const tool$ticketingRemoveTicketingNote: ToolDefinition<typeof args> = {
  name: "ticketing-remove-ticketing-note",
  description: `Remove a note`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingRemoveTicketingNote(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
