/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pageListKmsPages } from "../../funcs/pageListKmsPages.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListKmsPagesRequest$inboundSchema,
};

export const tool$pageListKmsPages: ToolDefinition<typeof args> = {
  name: "page-list-kms-pages",
  description: `List all pages`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await pageListKmsPages(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
