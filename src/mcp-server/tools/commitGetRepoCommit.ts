/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commitGetRepoCommit } from "../../funcs/commitGetRepoCommit.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetRepoCommitRequest$inboundSchema,
};

export const tool$commitGetRepoCommit: ToolDefinition<typeof args> = {
  name: "commit-get-repo-commit",
  description: `Retrieve a commit`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await commitGetRepoCommit(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
