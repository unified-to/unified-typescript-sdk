/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyGetHrisCompany } from "../../funcs/companyGetHrisCompany.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetHrisCompanyRequest$inboundSchema,
};

export const tool$companyGetHrisCompany: ToolDefinition<typeof args> = {
  name: "company-get-hris-company",
  description: `Retrieve a company`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyGetHrisCompany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
