/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contactListCrmContacts } from "../../funcs/contactListCrmContacts.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCrmContactsRequest$inboundSchema,
};

export const tool$contactListCrmContacts: ToolDefinition<typeof args> = {
  name: "contact-list-crm-contacts",
  description: `List all contacts`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contactListCrmContacts(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
