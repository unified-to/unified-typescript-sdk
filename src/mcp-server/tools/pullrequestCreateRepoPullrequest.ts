/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pullrequestCreateRepoPullrequest } from "../../funcs/pullrequestCreateRepoPullrequest.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateRepoPullrequestRequest$inboundSchema,
};

export const tool$pullrequestCreateRepoPullrequest: ToolDefinition<
  typeof args
> = {
  name: "pullrequest-create-repo-pullrequest",
  description: `Create a pullrequest`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await pullrequestCreateRepoPullrequest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
