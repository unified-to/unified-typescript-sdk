/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyUpdateCrmCompany } from "../../funcs/companyUpdateCrmCompany.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateCrmCompanyRequest$inboundSchema,
};

export const tool$companyUpdateCrmCompany: ToolDefinition<typeof args> = {
  name: "company-update-crm-company",
  description: `Update a company`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyUpdateCrmCompany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
