/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commentGetTaskComment } from "../../funcs/commentGetTaskComment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTaskCommentRequest$inboundSchema,
};

export const tool$commentGetTaskComment: ToolDefinition<typeof args> = {
  name: "comment-get-task-comment",
  description: `Retrieve a comment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await commentGetTaskComment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
