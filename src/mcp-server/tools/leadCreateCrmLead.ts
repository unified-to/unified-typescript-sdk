/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { leadCreateCrmLead } from "../../funcs/leadCreateCrmLead.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCrmLeadRequest$inboundSchema,
};

export const tool$leadCreateCrmLead: ToolDefinition<typeof args> = {
  name: "lead-create-crm-lead",
  description: `Create a lead`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await leadCreateCrmLead(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
