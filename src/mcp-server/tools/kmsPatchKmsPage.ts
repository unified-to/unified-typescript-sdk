/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kmsPatchKmsPage } from "../../funcs/kmsPatchKmsPage.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchKmsPageRequest$inboundSchema,
};

export const tool$kmsPatchKmsPage: ToolDefinition<typeof args> = {
  name: "kms-patch-kms-page",
  description: `Update a page`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kmsPatchKmsPage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
