/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { branchGetRepoBranch } from "../../funcs/branchGetRepoBranch.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetRepoBranchRequest$inboundSchema,
};

export const tool$branchGetRepoBranch: ToolDefinition<typeof args> = {
  name: "branch-get-repo-branch",
  description: `Retrieve a branch`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await branchGetRepoBranch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
