/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmGetCrmContact } from "../../funcs/crmGetCrmContact.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCrmContactRequest$inboundSchema,
};

export const tool$crmGetCrmContact: ToolDefinition<typeof args> = {
  name: "crm-get-crm-contact",
  description: `Retrieve a contact`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await crmGetCrmContact(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
