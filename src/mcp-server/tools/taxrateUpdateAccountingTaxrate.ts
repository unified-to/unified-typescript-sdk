/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taxrateUpdateAccountingTaxrate } from "../../funcs/taxrateUpdateAccountingTaxrate.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAccountingTaxrateRequest$inboundSchema,
};

export const tool$taxrateUpdateAccountingTaxrate: ToolDefinition<typeof args> =
  {
    name: "taxrate-update-accounting-taxrate",
    description: `Update a taxrate`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await taxrateUpdateAccountingTaxrate(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
