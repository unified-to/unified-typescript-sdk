/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { genaiCreateGenaiPrompt } from "../../funcs/genaiCreateGenaiPrompt.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateGenaiPromptRequest$inboundSchema,
};

export const tool$genaiCreateGenaiPrompt: ToolDefinition<typeof args> = {
  name: "genai-create-genai-prompt",
  description: `Create a prompt`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await genaiCreateGenaiPrompt(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
