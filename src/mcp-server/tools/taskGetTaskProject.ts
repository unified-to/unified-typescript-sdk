/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taskGetTaskProject } from "../../funcs/taskGetTaskProject.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTaskProjectRequest$inboundSchema,
};

export const tool$taskGetTaskProject: ToolDefinition<typeof args> = {
  name: "task-get-task-project",
  description: `Retrieve a project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await taskGetTaskProject(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
