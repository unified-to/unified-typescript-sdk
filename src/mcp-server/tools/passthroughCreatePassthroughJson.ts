/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passthroughCreatePassthroughJson } from "../../funcs/passthroughCreatePassthroughJson.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreatePassthroughJsonRequest$inboundSchema,
};

export const tool$passthroughCreatePassthroughJson: ToolDefinition<
  typeof args
> = {
  name: "passthrough-create-passthrough-json",
  description: `Passthrough POST`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await passthroughCreatePassthroughJson(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value?.result;

    return formatResult(value, apiCall);
  },
};
