/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingUpdateAccountingSalesorder } from "../../funcs/accountingUpdateAccountingSalesorder.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAccountingSalesorderRequest$inboundSchema,
};

export const tool$accountingUpdateAccountingSalesorder: ToolDefinition<
  typeof args
> = {
  name: "accounting-update-accounting-salesorder",
  description: `Update a salesorder`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingUpdateAccountingSalesorder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
