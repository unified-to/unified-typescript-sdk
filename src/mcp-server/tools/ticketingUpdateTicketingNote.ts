/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ticketingUpdateTicketingNote } from "../../funcs/ticketingUpdateTicketingNote.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateTicketingNoteRequest$inboundSchema,
};

export const tool$ticketingUpdateTicketingNote: ToolDefinition<typeof args> = {
  name: "ticketing-update-ticketing-note",
  description: `Update a note`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ticketingUpdateTicketingNote(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
