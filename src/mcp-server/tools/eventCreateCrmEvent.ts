/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventCreateCrmEvent } from "../../funcs/eventCreateCrmEvent.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCrmEventRequest$inboundSchema,
};

export const tool$eventCreateCrmEvent: ToolDefinition<typeof args> = {
  name: "event-create-crm-event",
  description: `Create an event`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await eventCreateCrmEvent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
