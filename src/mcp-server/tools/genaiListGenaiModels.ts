/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { genaiListGenaiModels } from "../../funcs/genaiListGenaiModels.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListGenaiModelsRequest$inboundSchema,
};

export const tool$genaiListGenaiModels: ToolDefinition<typeof args> = {
  name: "genai-list-genai-models",
  description: `List all models`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await genaiListGenaiModels(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
