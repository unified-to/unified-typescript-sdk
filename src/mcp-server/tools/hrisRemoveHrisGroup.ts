/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisRemoveHrisGroup } from "../../funcs/hrisRemoveHrisGroup.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveHrisGroupRequest$inboundSchema,
};

export const tool$hrisRemoveHrisGroup: ToolDefinition<typeof args> = {
  name: "hris-remove-hris-group",
  description: `Remove a group`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await hrisRemoveHrisGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
