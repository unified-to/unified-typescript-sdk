/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingCreateAccountingInvoice } from "../../funcs/accountingCreateAccountingInvoice.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateAccountingInvoiceRequest$inboundSchema,
};

export const tool$accountingCreateAccountingInvoice: ToolDefinition<
  typeof args
> = {
  name: "accounting-create-accounting-invoice",
  description: `Create an invoice`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingCreateAccountingInvoice(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
