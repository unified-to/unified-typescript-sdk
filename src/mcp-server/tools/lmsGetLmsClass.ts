/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lmsGetLmsClass } from "../../funcs/lmsGetLmsClass.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetLmsClassRequest$inboundSchema,
};

export const tool$lmsGetLmsClass: ToolDefinition<typeof args> = {
  name: "lms-get-lms-class",
  description: `Retrieve a class`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lmsGetLmsClass(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
