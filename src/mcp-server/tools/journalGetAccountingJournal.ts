/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { journalGetAccountingJournal } from "../../funcs/journalGetAccountingJournal.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAccountingJournalRequest$inboundSchema,
};

export const tool$journalGetAccountingJournal: ToolDefinition<typeof args> = {
  name: "journal-get-accounting-journal",
  description: `Retrieve a journal`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await journalGetAccountingJournal(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
