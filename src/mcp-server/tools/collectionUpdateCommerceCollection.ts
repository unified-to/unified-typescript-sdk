/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectionUpdateCommerceCollection } from "../../funcs/collectionUpdateCommerceCollection.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateCommerceCollectionRequest$inboundSchema,
};

export const tool$collectionUpdateCommerceCollection: ToolDefinition<
  typeof args
> = {
  name: "collection-update-commerce-collection",
  description: `Update a collection`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectionUpdateCommerceCollection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
