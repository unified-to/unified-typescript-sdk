/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { linkRemoveCalendarLink } from "../../funcs/linkRemoveCalendarLink.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveCalendarLinkRequest$inboundSchema,
};

export const tool$linkRemoveCalendarLink: ToolDefinition<typeof args> = {
  name: "link-remove-calendar-link",
  description: `Remove a link`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await linkRemoveCalendarLink(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
