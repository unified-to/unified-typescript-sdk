/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deviceGetHrisDevice } from "../../funcs/deviceGetHrisDevice.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetHrisDeviceRequest$inboundSchema,
};

export const tool$deviceGetHrisDevice: ToolDefinition<typeof args> = {
  name: "device-get-hris-device",
  description: `Retrieve a device`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deviceGetHrisDevice(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
