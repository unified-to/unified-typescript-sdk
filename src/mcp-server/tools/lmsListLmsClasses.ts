/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lmsListLmsClasses } from "../../funcs/lmsListLmsClasses.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListLmsClassesRequest$inboundSchema,
};

export const tool$lmsListLmsClasses: ToolDefinition<typeof args> = {
  name: "lms-list-lms-classes",
  description: `List all classes`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lmsListLmsClasses(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
