/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ucRemoveUcRecording } from "../../funcs/ucRemoveUcRecording.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveUcRecordingRequest$inboundSchema,
};

export const tool$ucRemoveUcRecording: ToolDefinition<typeof args> = {
  name: "uc-remove-uc-recording",
  description: `Remove a recording`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ucRemoveUcRecording(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
