/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pipelineCreateCrmPipeline } from "../../funcs/pipelineCreateCrmPipeline.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCrmPipelineRequest$inboundSchema,
};

export const tool$pipelineCreateCrmPipeline: ToolDefinition<typeof args> = {
  name: "pipeline-create-crm-pipeline",
  description: `Create a pipeline`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await pipelineCreateCrmPipeline(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
