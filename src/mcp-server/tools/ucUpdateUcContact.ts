/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ucUpdateUcContact } from "../../funcs/ucUpdateUcContact.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateUcContactRequest$inboundSchema,
};

export const tool$ucUpdateUcContact: ToolDefinition<typeof args> = {
  name: "uc-update-uc-contact",
  description: `Update a contact`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ucUpdateUcContact(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
