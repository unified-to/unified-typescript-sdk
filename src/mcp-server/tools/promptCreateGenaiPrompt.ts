/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { promptCreateGenaiPrompt } from "../../funcs/promptCreateGenaiPrompt.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateGenaiPromptRequest$inboundSchema,
};

export const tool$promptCreateGenaiPrompt: ToolDefinition<typeof args> = {
  name: "prompt-create-genai-prompt",
  description: `Create a prompt`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await promptCreateGenaiPrompt(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
