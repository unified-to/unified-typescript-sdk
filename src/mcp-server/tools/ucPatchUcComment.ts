/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ucPatchUcComment } from "../../funcs/ucPatchUcComment.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchUcCommentRequest$inboundSchema,
};

export const tool$ucPatchUcComment: ToolDefinition<typeof args> = {
  name: "uc-patch-uc-comment",
  description: `Update a comment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await ucPatchUcComment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
