/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectionUpdateUnifiedConnection } from "../../funcs/connectionUpdateUnifiedConnection.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateUnifiedConnectionRequest$inboundSchema,
};

export const tool$connectionUpdateUnifiedConnection: ToolDefinition<
  typeof args
> = {
  name: "connection-update-unified-connection",
  description: `Update connection`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await connectionUpdateUnifiedConnection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
