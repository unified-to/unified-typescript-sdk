/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentGetPaymentLink } from "../../funcs/paymentGetPaymentLink.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPaymentLinkRequest$inboundSchema,
};

export const tool$paymentGetPaymentLink: ToolDefinition<typeof args> = {
  name: "payment-get-payment-link",
  description: `Retrieve a link`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentGetPaymentLink(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
