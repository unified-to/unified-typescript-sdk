/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { calendarPatchCalendarCalendar } from "../../funcs/calendarPatchCalendarCalendar.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchCalendarCalendarRequest$inboundSchema,
};

export const tool$calendarPatchCalendarCalendar: ToolDefinition<typeof args> = {
  name: "calendar-patch-calendar-calendar",
  description: `Update a calendar`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await calendarPatchCalendarCalendar(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
