/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { verificationGetVerificationRequest } from "../../funcs/verificationGetVerificationRequest.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetVerificationRequestRequest$inboundSchema,
};

export const tool$verificationGetVerificationRequest: ToolDefinition<
  typeof args
> = {
  name: "verification-get-verification-request",
  description: `Retrieve a request`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await verificationGetVerificationRequest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
