/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { transactionRemoveAccountingTransaction } from "../../funcs/transactionRemoveAccountingTransaction.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveAccountingTransactionRequest$inboundSchema,
};

export const tool$transactionRemoveAccountingTransaction: ToolDefinition<
  typeof args
> = {
  name: "transaction-remove-accounting-transaction",
  description: `Remove a transaction`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await transactionRemoveAccountingTransaction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
