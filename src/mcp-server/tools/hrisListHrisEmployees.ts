/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisListHrisEmployees } from "../../funcs/hrisListHrisEmployees.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListHrisEmployeesRequest$inboundSchema,
};

export const tool$hrisListHrisEmployees: ToolDefinition<typeof args> = {
  name: "hris-list-hris-employees",
  description: `List all employees`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await hrisListHrisEmployees(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
