/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { payoutGetPaymentPayout } from "../../funcs/payoutGetPaymentPayout.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPaymentPayoutRequest$inboundSchema,
};

export const tool$payoutGetPaymentPayout: ToolDefinition<typeof args> = {
  name: "payout-get-payment-payout",
  description: `Retrieve a payout`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await payoutGetPaymentPayout(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
