/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repoPatchRepoCommit } from "../../funcs/repoPatchRepoCommit.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchRepoCommitRequest$inboundSchema,
};

export const tool$repoPatchRepoCommit: ToolDefinition<typeof args> = {
  name: "repo-patch-repo-commit",
  description: `Update a commit`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await repoPatchRepoCommit(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
