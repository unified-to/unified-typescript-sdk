/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invoiceRemoveAccountingInvoice } from "../../funcs/invoiceRemoveAccountingInvoice.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveAccountingInvoiceRequest$inboundSchema,
};

export const tool$invoiceRemoveAccountingInvoice: ToolDefinition<typeof args> =
  {
    name: "invoice-remove-accounting-invoice",
    description: `Remove an invoice`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await invoiceRemoveAccountingInvoice(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
