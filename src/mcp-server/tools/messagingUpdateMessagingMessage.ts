/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { messagingUpdateMessagingMessage } from "../../funcs/messagingUpdateMessagingMessage.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateMessagingMessageRequest$inboundSchema,
};

export const tool$messagingUpdateMessagingMessage: ToolDefinition<typeof args> =
  {
    name: "messaging-update-messaging-message",
    description: `Update a message`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await messagingUpdateMessagingMessage(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
