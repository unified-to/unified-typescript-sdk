/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyListHrisCompanies } from "../../funcs/companyListHrisCompanies.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListHrisCompaniesRequest$inboundSchema,
};

export const tool$companyListHrisCompanies: ToolDefinition<typeof args> = {
  name: "company-list-hris-companies",
  description: `List all companies`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyListHrisCompanies(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
