/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentGetPaymentRefund } from "../../funcs/paymentGetPaymentRefund.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPaymentRefundRequest$inboundSchema,
};

export const tool$paymentGetPaymentRefund: ToolDefinition<typeof args> = {
  name: "payment-get-payment-refund",
  description: `Retrieve a refund`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentGetPaymentRefund(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
