/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { kmsListKmsPages } from "../../funcs/kmsListKmsPages.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListKmsPagesRequest$inboundSchema,
};

export const tool$kmsListKmsPages: ToolDefinition<typeof args> = {
  name: "kms-list-kms-pages",
  description: `List all pages`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await kmsListKmsPages(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
