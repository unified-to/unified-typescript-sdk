/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { genaiCreateGenaiEmbedding } from "../../funcs/genaiCreateGenaiEmbedding.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateGenaiEmbeddingRequest$inboundSchema,
};

export const tool$genaiCreateGenaiEmbedding: ToolDefinition<typeof args> = {
  name: "genai-create-genai-embedding",
  description: `Create an embedding`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await genaiCreateGenaiEmbedding(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
