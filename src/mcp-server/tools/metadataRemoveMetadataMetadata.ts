/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metadataRemoveMetadataMetadata } from "../../funcs/metadataRemoveMetadataMetadata.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveMetadataMetadataRequest$inboundSchema,
};

export const tool$metadataRemoveMetadataMetadata: ToolDefinition<typeof args> =
  {
    name: "metadata-remove-metadata-metadata",
    description: `Remove a metadata`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await metadataRemoveMetadataMetadata(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
