/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { verificationGetVerificationPackage } from "../../funcs/verificationGetVerificationPackage.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetVerificationPackageRequest$inboundSchema,
};

export const tool$verificationGetVerificationPackage: ToolDefinition<
  typeof args
> = {
  name: "verification-get-verification-package",
  description: `Retrieve a package`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await verificationGetVerificationPackage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
