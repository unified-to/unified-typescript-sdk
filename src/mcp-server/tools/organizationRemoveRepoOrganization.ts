/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationRemoveRepoOrganization } from "../../funcs/organizationRemoveRepoOrganization.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveRepoOrganizationRequest$inboundSchema,
};

export const tool$organizationRemoveRepoOrganization: ToolDefinition<
  typeof args
> = {
  name: "organization-remove-repo-organization",
  description: `Remove an organization`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationRemoveRepoOrganization(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
