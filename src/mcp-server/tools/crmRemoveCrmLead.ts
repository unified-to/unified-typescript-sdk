/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmRemoveCrmLead } from "../../funcs/crmRemoveCrmLead.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveCrmLeadRequest$inboundSchema,
};

export const tool$crmRemoveCrmLead: ToolDefinition<typeof args> = {
  name: "crm-remove-crm-lead",
  description: `Remove a lead`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await crmRemoveCrmLead(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
