/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contactCreateAccountingContact } from "../../funcs/contactCreateAccountingContact.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateAccountingContactRequest$inboundSchema,
};

export const tool$contactCreateAccountingContact: ToolDefinition<typeof args> =
  {
    name: "contact-create-accounting-contact",
    description: `Create a contact`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await contactCreateAccountingContact(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
