/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lmsCreateLmsStudent } from "../../funcs/lmsCreateLmsStudent.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateLmsStudentRequest$inboundSchema,
};

export const tool$lmsCreateLmsStudent: ToolDefinition<typeof args> = {
  name: "lms-create-lms-student",
  description: `Create a student`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lmsCreateLmsStudent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
