/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contactRemoveAccountingContact } from "../../funcs/contactRemoveAccountingContact.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveAccountingContactRequest$inboundSchema,
};

export const tool$contactRemoveAccountingContact: ToolDefinition<typeof args> =
  {
    name: "contact-remove-accounting-contact",
    description: `Remove a contact`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await contactRemoveAccountingContact(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
