/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repoRemoveRepoRepository } from "../../funcs/repoRemoveRepoRepository.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveRepoRepositoryRequest$inboundSchema,
};

export const tool$repoRemoveRepoRepository: ToolDefinition<typeof args> = {
  name: "repo-remove-repo-repository",
  description: `Remove a repository`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await repoRemoveRepoRepository(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
