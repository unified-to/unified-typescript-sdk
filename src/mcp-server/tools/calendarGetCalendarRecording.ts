/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { calendarGetCalendarRecording } from "../../funcs/calendarGetCalendarRecording.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCalendarRecordingRequest$inboundSchema,
};

export const tool$calendarGetCalendarRecording: ToolDefinition<typeof args> = {
  name: "calendar-get-calendar-recording",
  description: `Retrieve a recording`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await calendarGetCalendarRecording(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
