/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingUpdateAccountingCreditmemo } from "../../funcs/accountingUpdateAccountingCreditmemo.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAccountingCreditmemoRequest$inboundSchema,
};

export const tool$accountingUpdateAccountingCreditmemo: ToolDefinition<
  typeof args
> = {
  name: "accounting-update-accounting-creditmemo",
  description: `Update a creditmemo`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountingUpdateAccountingCreditmemo(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
