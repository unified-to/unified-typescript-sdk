/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { repoListRepoBranches } from "../../funcs/repoListRepoBranches.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListRepoBranchesRequest$inboundSchema,
};

export const tool$repoListRepoBranches: ToolDefinition<typeof args> = {
  name: "repo-list-repo-branches",
  description: `List all branches`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await repoListRepoBranches(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
