/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../../lib/primitives.js";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type RemoveMartechCampaignRequest = {
  /**
   * ID of the connection
   */
  connectionId: string;
  /**
   * ID of the Campaign
   */
  id: string;
};

export type RemoveMartechCampaignResponse = {
  headers: { [k: string]: Array<string> };
};

/** @internal */
export type RemoveMartechCampaignRequest$Outbound = {
  connection_id: string;
  id: string;
};

/** @internal */
export const RemoveMartechCampaignRequest$outboundSchema: z.ZodType<
  RemoveMartechCampaignRequest$Outbound,
  z.ZodTypeDef,
  RemoveMartechCampaignRequest
> = z.object({
  connectionId: z.string(),
  id: z.string(),
}).transform((v) => {
  return remap$(v, {
    connectionId: "connection_id",
  });
});

export function removeMartechCampaignRequestToJSON(
  removeMartechCampaignRequest: RemoveMartechCampaignRequest,
): string {
  return JSON.stringify(
    RemoveMartechCampaignRequest$outboundSchema.parse(
      removeMartechCampaignRequest,
    ),
  );
}

/** @internal */
export const RemoveMartechCampaignResponse$inboundSchema: z.ZodType<
  RemoveMartechCampaignResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Headers: z.record(z.array(z.string())).default({}),
}).transform((v) => {
  return remap$(v, {
    "Headers": "headers",
  });
});

export function removeMartechCampaignResponseFromJSON(
  jsonString: string,
): SafeParseResult<RemoveMartechCampaignResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RemoveMartechCampaignResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RemoveMartechCampaignResponse' from JSON`,
  );
}
