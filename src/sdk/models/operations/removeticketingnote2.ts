/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../../lib/primitives.js";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type RemoveTicketingNote2Request = {
  /**
   * ID of the connection
   */
  connectionId: string;
  /**
   * ID of the Note
   */
  id: string;
};

export type RemoveTicketingNote2Response = {
  headers: { [k: string]: Array<string> };
};

/** @internal */
export type RemoveTicketingNote2Request$Outbound = {
  connection_id: string;
  id: string;
};

/** @internal */
export const RemoveTicketingNote2Request$outboundSchema: z.ZodType<
  RemoveTicketingNote2Request$Outbound,
  z.ZodTypeDef,
  RemoveTicketingNote2Request
> = z.object({
  connectionId: z.string(),
  id: z.string(),
}).transform((v) => {
  return remap$(v, {
    connectionId: "connection_id",
  });
});

export function removeTicketingNote2RequestToJSON(
  removeTicketingNote2Request: RemoveTicketingNote2Request,
): string {
  return JSON.stringify(
    RemoveTicketingNote2Request$outboundSchema.parse(
      removeTicketingNote2Request,
    ),
  );
}

/** @internal */
export const RemoveTicketingNote2Response$inboundSchema: z.ZodType<
  RemoveTicketingNote2Response,
  z.ZodTypeDef,
  unknown
> = z.object({
  Headers: z.record(z.array(z.string())).default({}),
}).transform((v) => {
  return remap$(v, {
    "Headers": "headers",
  });
});

export function removeTicketingNote2ResponseFromJSON(
  jsonString: string,
): SafeParseResult<RemoveTicketingNote2Response, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RemoveTicketingNote2Response$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RemoveTicketingNote2Response' from JSON`,
  );
}
